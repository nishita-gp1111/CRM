generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum OrganizationRole {
  SUPER_ADMIN @map("super_admin")
  ADMIN       @map("admin")
  MANAGER     @map("manager")
  USER        @map("user")
  READ_ONLY   @map("read_only")

  @@map("organization_role")
}

enum MemberStatus {
  INVITED   @map("invited")
  ACTIVE    @map("active")
  SUSPENDED @map("suspended")

  @@map("member_status")
}

enum BusinessUnitStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")

  @@map("business_unit_status")
}

enum WorkFunction {
  IS
  FS
  CS

  @@map("work_function")
}

enum StageType {
  OPEN @map("open")
  WON  @map("won")
  LOST @map("lost")

  @@map("stage_type")
}

enum DealStatus {
  OPEN      @map("open")
  WON       @map("won")
  LOST      @map("lost")
  CANCELLED @map("cancelled")
  INVALID   @map("invalid")
  NURTURE   @map("nurture")

  @@map("deal_status")
}

enum ProductStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")
  ARCHIVED @map("archived")

  @@map("product_status")
}

enum PriceBookStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")

  @@map("price_book_status")
}

enum DealLineItemStatus {
  PROPOSED     @map("proposed")
  WON          @map("won")
  LOST         @map("lost")
  CANCELLED    @map("cancelled")
  NOT_SELECTED @map("not_selected")

  @@map("deal_line_item_status")
}

enum AmountMetricBasis {
  REVENUE      @map("revenue")
  GROSS_PROFIT @map("gross_profit")

  @@map("amount_metric_basis")
}

enum ConfirmedAmountDateBasis {
  WON_AT             @map("won_at")
  CONTRACTED_AT      @map("contracted_at")
  COLLECTED_AT       @map("collected_at")
  BILLING_STARTED_AT @map("billing_started_at")

  @@map("confirmed_amount_date_basis")
}

enum ProductKind {
  CORE       @map("core")
  ADD_ON     @map("add_on")
  OPTIONAL   @map("optional")
  CROSS_SELL @map("cross_sell")

  @@map("product_kind")
}

enum AttachmentDenominatorMode {
  ALL_WON_DEALS           @map("all_won_deals")
  DEALS_WITH_BASE_PRODUCT @map("deals_with_base_product")
  DEALS_MATCHING_FILTER   @map("deals_matching_filter")

  @@map("attachment_denominator_mode")
}

enum LossReasonScope {
  DEAL           @map("deal")
  DEAL_LINE_ITEM @map("deal_line_item")
  BOTH           @map("both")

  @@map("loss_reason_scope")
}

enum LossReasonStatus {
  LOST         @map("lost")
  CANCELLED    @map("cancelled")
  INVALID      @map("invalid")
  NOT_SELECTED @map("not_selected")

  @@map("loss_reason_status")
}

enum DealAlertRuleType {
  MEETING_OVERDUE           @map("meeting_overdue")
  NEXT_ACTION_OVERDUE       @map("next_action_overdue")
  NO_ACTIVITY_DAYS          @map("no_activity_days")
  STAGE_STALE_DAYS          @map("stage_stale_days")
  MISSING_LINE_ITEMS        @map("missing_line_items")
  MISSING_CLOSER            @map("missing_closer")
  MISSING_FORECAST_CATEGORY @map("missing_forecast_category")
  MISSING_EXPECTED_AMOUNT   @map("missing_expected_amount")

  @@map("deal_alert_rule_type")
}

enum ForecastCategoryStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")

  @@map("forecast_category_status")
}

enum DecisionMakerStatus {
  DECISION_MAKER     @map("decision_maker")
  NON_DECISION_MAKER @map("non_decision_maker")
  UNKNOWN            @map("unknown")

  @@map("decision_maker_status")
}

enum QualificationResult {
  VALID        @map("valid")
  INVALID      @map("invalid")
  CONDITION_NG @map("condition_ng")
  UNDETERMINED @map("undetermined")

  @@map("qualification_result")
}

enum DealParticipantRole {
  OWNER                 @map("owner")
  APPOINTMENT_SETTER    @map("appointment_setter")
  CLOSER                @map("closer")
  REFERRER              @map("referrer")
  WALK_IN_OWNER         @map("walk_in_owner")
  SUPPORT               @map("support")
  CROSS_SELL_ORIGINATOR @map("cross_sell_originator")
  MEETING_OWNER         @map("meeting_owner")

  @@map("deal_participant_role")
}

enum DealType {
  NEW_BUSINESS @map("new_business")
  CROSS_SELL   @map("cross_sell")

  @@map("deal_type")
}

enum FulfillmentType {
  NONE              @map("none")
  PROJECT           @map("project")
  RECURRING_SERVICE @map("recurring_service")

  @@map("fulfillment_type")
}

enum ProjectGroupingMode {
  GROUP_BY_DEAL         @map("group_by_deal")
  SEPARATE_BY_LINE_ITEM @map("separate_by_line_item")

  @@map("project_grouping_mode")
}

enum DeliveryProjectStatus {
  NOT_STARTED @map("not_started")
  IN_PROGRESS @map("in_progress")
  PAUSED      @map("paused")
  PUBLISHED   @map("published")
  COMPLETED   @map("completed")
  CANCELLED   @map("cancelled")

  @@map("delivery_project_status")
}

enum DeliveryHealthStatus {
  ON_TRACK  @map("on_track")
  AT_RISK   @map("at_risk")
  OFF_TRACK @map("off_track")
  BLOCKED   @map("blocked")

  @@map("delivery_health_status")
}

enum DeliveryPriority {
  LOW    @map("low")
  MEDIUM @map("medium")
  HIGH   @map("high")
  URGENT @map("urgent")

  @@map("delivery_priority")
}

enum DeliveryHandoffStatus {
  DRAFT     @map("draft")
  READY     @map("ready")
  ACCEPTED  @map("accepted")
  REJECTED  @map("rejected")
  COMPLETED @map("completed")

  @@map("delivery_handoff_status")
}

enum ScopeSyncStatus {
  SYNCED          @map("synced")
  SOURCE_CHANGED  @map("source_changed")
  REVIEW_REQUIRED @map("review_required")

  @@map("scope_sync_status")
}

enum DeliveryItemStatus {
  NOT_STARTED @map("not_started")
  IN_PROGRESS @map("in_progress")
  DELIVERED   @map("delivered")
  CANCELLED   @map("cancelled")

  @@map("delivery_item_status")
}

enum DeliveryStageType {
  NORMAL    @map("normal")
  PUBLISHED @map("published")
  COMPLETED @map("completed")
  PAUSED    @map("paused")

  @@map("delivery_stage_type")
}

enum DealParticipantStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")

  @@map("deal_participant_status")
}

enum MeetingBookingStatus {
  SCHEDULED   @map("scheduled")
  ATTENDED    @map("attended")
  NO_SHOW     @map("no_show")
  CANCELLED   @map("cancelled")
  RESCHEDULED @map("rescheduled")
  INVALID     @map("invalid")

  @@map("meeting_booking_status")
}

enum FormVersionStatus {
  DRAFT     @map("draft")
  PUBLISHED @map("published")
  ARCHIVED  @map("archived")

  @@map("form_version_status")
}

enum FormStatus {
  DRAFT     @map("draft")
  PUBLISHED @map("published")
  PAUSED    @map("paused")
  ARCHIVED  @map("archived")

  @@map("form_status")
}

enum FormPurpose {
  PUBLIC_INQUIRY       @map("public_inquiry")
  PUBLIC_SCHEDULER     @map("public_scheduler")
  INTERNAL_APPOINTMENT @map("internal_appointment")
  INTERNAL_HANDOFF     @map("internal_handoff")

  @@map("form_purpose")
}

enum RoutingRuleStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")

  @@map("routing_rule_status")
}

enum RoutingConditionJoin {
  AND @map("and")
  OR  @map("or")

  @@map("routing_condition_join")
}

enum AssignmentMode {
  MANUAL           @map("manual")
  FIXED_USER       @map("fixed_user")
  ROUND_ROBIN      @map("round_robin")
  TEAM_ROUND_ROBIN @map("team_round_robin")

  @@map("assignment_mode")
}

enum MeetingLinkStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")
  PAUSED   @map("paused")

  @@map("meeting_link_status")
}

enum MeetingLocationType {
  PHONE       @map("phone")
  IN_PERSON   @map("in_person")
  GOOGLE_MEET @map("google_meet")
  CUSTOM_URL  @map("custom_url")
  OTHER       @map("other")

  @@map("meeting_location_type")
}

enum BookingStatus {
  HOLD         @map("hold")
  PENDING_SYNC @map("pending_sync")
  CONFIRMED    @map("confirmed")
  RESCHEDULED  @map("rescheduled")
  CANCELLED    @map("cancelled")
  ATTENDED     @map("attended")
  NO_SHOW      @map("no_show")

  @@map("booking_status")
}

enum CalendarSyncStatus {
  NOT_REQUIRED             @map("not_required")
  PENDING                  @map("pending")
  SYNCED                   @map("synced")
  RETRY_PENDING            @map("retry_pending")
  ERROR                    @map("error")
  REAUTH_REQUIRED          @map("reauth_required")
  EXTERNAL_CHANGE_DETECTED @map("external_change_detected")
  REVIEW_REQUIRED          @map("review_required")

  @@map("calendar_sync_status")
}

enum BookingOrigin {
  PUBLIC_FORM      @map("public_form")
  PUBLIC_SCHEDULER @map("public_scheduler")
  INTERNAL         @map("internal")
  IMPORT           @map("import")
  MANUAL           @map("manual")

  @@map("booking_origin")
}

enum AppointmentCreditPolicy {
  ASSIGNED_USER @map("assigned_user")
  FORM_OWNER    @map("form_owner")
  NO_IS_CREDIT  @map("no_is_credit")
  FIXED_USER    @map("fixed_user")

  @@map("appointment_credit_policy")
}

enum GoogleCalendarConnectionStatus {
  CONNECTED       @map("connected")
  REAUTH_REQUIRED @map("reauth_required")
  REVOKED         @map("revoked")
  ERROR           @map("error")

  @@map("google_calendar_connection_status")
}

enum BookingHoldStatus {
  ACTIVE    @map("active")
  CONSUMED  @map("consumed")
  EXPIRED   @map("expired")
  CANCELLED @map("cancelled")

  @@map("booking_hold_status")
}

enum WatchChannelStatus {
  ACTIVE  @map("active")
  EXPIRED @map("expired")
  STOPPED @map("stopped")
  ERROR   @map("error")

  @@map("watch_channel_status")
}

enum CalendarExternalChangeType {
  DELETED              @map("deleted")
  DATE_TIME_CHANGED    @map("date_time_changed")
  PARTICIPANTS_CHANGED @map("participants_changed")
  TITLE_CHANGED        @map("title_changed")
  UNKNOWN              @map("unknown")

  @@map("calendar_external_change_type")
}

enum CalendarSyncJobStatus {
  PENDING    @map("pending")
  RUNNING    @map("running")
  SUCCEEDED  @map("succeeded")
  FAILED     @map("failed")
  SKIPPED    @map("skipped")

  @@map("calendar_sync_job_status")
}

enum CalendarSyncJobType {
  FULL_SYNC        @map("full_sync")
  INCREMENTAL_SYNC @map("incremental_sync")
  WATCH_RENEWAL    @map("watch_renewal")
  WEBHOOK_SYNC      @map("webhook_sync")
  MANUAL_SYNC       @map("manual_sync")

  @@map("calendar_sync_job_type")
}

enum OperationalEventType {
  FORM_SUBMISSION_SUCCEEDED       @map("form_submission_succeeded")
  FORM_SUBMISSION_FAILED          @map("form_submission_failed")
  BOOKING_SUCCEEDED               @map("booking_succeeded")
  BOOKING_CONFLICT_PREVENTED      @map("booking_conflict_prevented")
  GOOGLE_SYNC_SUCCEEDED           @map("google_sync_succeeded")
  GOOGLE_SYNC_FAILED              @map("google_sync_failed")
  GOOGLE_RETRY_SUCCEEDED          @map("google_retry_succeeded")
  GOOGLE_REAUTH_REQUIRED          @map("google_reauth_required")
  WEBHOOK_RECEIVED                @map("webhook_received")
  WEBHOOK_REJECTED                @map("webhook_rejected")
  WATCH_CHANNEL_EXPIRING          @map("watch_channel_expiring")
  WATCH_CHANNEL_RENEWED           @map("watch_channel_renewed")
  EXTERNAL_CHANGE_DETECTED        @map("external_change_detected")
  ROUND_ROBIN_ASSIGNED            @map("round_robin_assigned")
  ASSIGNMENT_FAILED               @map("assignment_failed")

  @@map("operational_event_type")
}

enum SalesPerformanceEventType {
  CALL                         @map("call")
  CONNECTION                   @map("connection")
  OWNER_CONTACT                @map("owner_contact")
  FULL                         @map("full")
  SHORT                        @map("short")
  CONDITION_NG                 @map("condition_ng")
  APPOINTMENT_SET              @map("appointment_set")
  VALID_MEETING                @map("valid_meeting")
  INVALID_MEETING              @map("invalid_meeting")
  MEETING_ATTENDED             @map("meeting_attended")
  DEAL_WON                     @map("deal_won")
  DEAL_LOST                    @map("deal_lost")
  REVENUE_RECOGNIZED           @map("revenue_recognized")
  GROSS_PROFIT_RECOGNIZED      @map("gross_profit_recognized")
  REFERRAL_CREATED             @map("referral_created")
  REFERRAL_APPOINTMENT_SET     @map("referral_appointment_set")
  REFERRAL_MEETING_ATTENDED    @map("referral_meeting_attended")
  REFERRAL_WON                 @map("referral_won")
  FIELD_VISIT                  @map("field_visit")
  FIELD_VISIT_CONNECTION       @map("field_visit_connection")
  FIELD_VISIT_OWNER_CONNECTION @map("field_visit_owner_connection")
  FIELD_VISIT_MEETING          @map("field_visit_meeting")
  FIELD_VISIT_APPOINTMENT_SET  @map("field_visit_appointment_set")
  FIELD_VISIT_WON              @map("field_visit_won")
  FIELD_VISIT_SAME_DAY_WON     @map("field_visit_same_day_won")
  DOMAIN_ATTACHMENT            @map("domain_attachment")
  CROSS_SELL_CREATED           @map("cross_sell_created")
  CROSS_SELL_MEETING_SET       @map("cross_sell_meeting_set")
  CROSS_SELL_MEETING_ATTENDED  @map("cross_sell_meeting_attended")
  CROSS_SELL_WON               @map("cross_sell_won")
  CROSS_SELL_CANCELLED         @map("cross_sell_cancelled")
  CROSS_SELL_LOST              @map("cross_sell_lost")
  CROSS_SELL_ORIGINATED_GP     @map("cross_sell_originated_gp")

  @@map("sales_performance_event_type")
}

enum SalesPerformanceEventSource {
  MANUAL   @map("manual")
  SYSTEM   @map("system")
  IMPORT   @map("import")
  BACKFILL @map("backfill")

  @@map("sales_performance_event_source")
}

enum ReferralStatus {
  NEW              @map("new")
  APPOINTMENT_SET  @map("appointment_set")
  MEETING_ATTENDED @map("meeting_attended")
  WON              @map("won")
  LOST             @map("lost")
  CANCELLED        @map("cancelled")

  @@map("referral_status")
}

enum FieldVisitStatus {
  PLANNED         @map("planned")
  VISITED         @map("visited")
  CONNECTED       @map("connected")
  OWNER_CONNECTED @map("owner_connected")
  MEETING_SET     @map("meeting_set")
  APPOINTMENT_SET @map("appointment_set")
  WON             @map("won")
  LOST            @map("lost")
  INVALID         @map("invalid")

  @@map("field_visit_status")
}

enum MetricSourceType {
  MANUAL_DAILY      @map("manual_daily")
  PERFORMANCE_EVENT @map("performance_event")
  APPOINTMENT       @map("appointment")
  DEAL              @map("deal")
  DEAL_LINE_ITEM    @map("deal_line_item")
  REFERRAL          @map("referral")
  FIELD_VISIT       @map("field_visit")
  FORMULA           @map("formula")
  DELIVERY_PROJECT  @map("delivery_project")

  @@map("metric_source_type")
}

enum MetricCategory {
  EXECUTIVE   @map("executive")
  OUTCOME     @map("outcome")
  PIPELINE    @map("pipeline")
  ACTIVITY    @map("activity")
  CONVERSION  @map("conversion")
  QUALITY     @map("quality")
  REFERRAL    @map("referral")
  FIELD_VISIT @map("field_visit")
  PRODUCT     @map("product")
  FORECAST    @map("forecast")
  ACTION_PLAN @map("action_plan")
  CS          @map("cs")

  @@map("metric_category")
}

enum MetricUnit {
  COUNT    @map("count")
  CURRENCY @map("currency")
  PERCENT  @map("percent")
  DAYS     @map("days")
  NUMBER   @map("number")

  @@map("metric_unit")
}

enum MetricAggregation {
  COUNT          @map("count")
  DISTINCT_COUNT @map("distinct_count")
  SUM            @map("sum")
  AVERAGE        @map("average")
  RATE           @map("rate")

  @@map("metric_aggregation")
}

enum MetricPeriodType {
  DAILY     @map("daily")
  WEEKLY    @map("weekly")
  MONTHLY   @map("monthly")
  QUARTERLY @map("quarterly")
  YEARLY    @map("yearly")
  CUSTOM    @map("custom")

  @@map("metric_period_type")
}

enum DailyMetricStatus {
  DRAFT     @map("draft")
  SUBMITTED @map("submitted")
  APPROVED  @map("approved")
  LOCKED    @map("locked")

  @@map("daily_metric_status")
}

enum DailyMetricSource {
  MANUAL     @map("manual")
  IMPORT     @map("import")
  BACKFILL   @map("backfill")
  ADJUSTMENT @map("adjustment")

  @@map("daily_metric_source")
}

enum ValidationSeverity {
  INFO    @map("info")
  WARNING @map("warning")
  ERROR   @map("error")

  @@map("validation_severity")
}

enum ActionPlanStatus {
  NOT_STARTED @map("not_started")
  IN_PROGRESS @map("in_progress")
  COMPLETED   @map("completed")
  CANCELLED   @map("cancelled")

  @@map("action_plan_status")
}

enum ActionPlanPriority {
  LOW    @map("low")
  MEDIUM @map("medium")
  HIGH   @map("high")
  URGENT @map("urgent")

  @@map("action_plan_priority")
}

enum ActivityType {
  NOTE             @map("note")
  EMAIL            @map("email")
  CALL             @map("call")
  MEETING          @map("meeting")
  TASK_CREATED     @map("task_created")
  TASK_COMPLETED   @map("task_completed")
  STAGE_CHANGED    @map("stage_changed")
  PROPERTY_UPDATED @map("property_updated")
  FORM_SUBMITTED   @map("form_submitted")
  CHAT_MESSAGE     @map("chat_message")
  SYSTEM_EVENT     @map("system_event")

  @@map("activity_type")
}

enum TaskStatus {
  TODO        @map("todo")
  IN_PROGRESS @map("in_progress")
  COMPLETED   @map("completed")
  CANCELED    @map("canceled")

  @@map("task_status")
}

enum TaskPriority {
  LOW    @map("low")
  MEDIUM @map("medium")
  HIGH   @map("high")

  @@map("task_priority")
}

enum TaskType {
  CALL      @map("call")
  EMAIL     @map("email")
  FOLLOW_UP @map("follow_up")
  MEETING   @map("meeting")
  OTHER     @map("other")

  @@map("task_type")
}

enum ObjectType {
  CONTACT  @map("contact")
  COMPANY  @map("company")
  DEAL     @map("deal")
  ACTIVITY @map("activity")
  TASK     @map("task")

  @@map("object_type")
}

enum CustomPropertyObjectType {
  CONTACT        @map("contact")
  COMPANY        @map("company")
  DEAL           @map("deal")
  DEAL_LINE_ITEM @map("deal_line_item")

  @@map("custom_property_object_type")
}

enum CustomFieldType {
  TEXT         @map("text")
  TEXTAREA     @map("textarea")
  NUMBER       @map("number")
  CURRENCY     @map("currency")
  PERCENTAGE   @map("percentage")
  DATE         @map("date")
  DATETIME     @map("datetime")
  SELECT       @map("select")
  MULTI_SELECT @map("multi_select")
  CHECKBOX     @map("checkbox")
  URL          @map("url")
  EMAIL        @map("email")
  PHONE        @map("phone")

  @@map("custom_field_type")
}

enum ImportStatus {
  PENDING    @map("pending")
  VALIDATING @map("validating")
  READY      @map("ready")
  PROCESSING @map("processing")
  COMPLETED  @map("completed")
  FAILED     @map("failed")

  @@map("import_status")
}

model User {
  id              String    @id @default(uuid()) @db.Uuid
  email           String    @unique @db.VarChar(320)
  name            String    @db.VarChar(120)
  passwordHash    String    @map("password_hash")
  emailVerifiedAt DateTime? @map("email_verified_at") @db.Timestamptz(3)
  createdAt       DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt       DateTime  @updatedAt @map("updated_at") @db.Timestamptz(3)

  memberships             OrganizationMember[]
  sessions                AuthSession[]
  ownedContacts           Contact[]                @relation("ContactOwner")
  ownedCompanies          Company[]                @relation("CompanyOwner")
  ownedDeals              Deal[]                   @relation("DealOwner")
  activities              Activity[]
  ownedTasks              Task[]                   @relation("TaskOwner")
  createdTasks            Task[]                   @relation("TaskCreator")
  uploadedImports         ImportJob[]
  createdTemplates        EmailTemplate[]
  availabilityRules       AvailabilityRule[]
  meetingLinks            MeetingLink[]
  auditLogs               AuditLog[]
  savedViews              SavedView[]
  businessUnitMemberships BusinessUnitMembership[]
}

model Organization {
  id        String   @id @default(uuid()) @db.Uuid
  name      String   @db.VarChar(160)
  slug      String   @unique @db.VarChar(80)
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  memberships                OrganizationMember[]
  teams                      Team[]
  invitations                Invitation[]
  sessions                   AuthSession[]
  contacts                   Contact[]
  companies                  Company[]
  deals                      Deal[]
  activities                 Activity[]
  tasks                      Task[]
  pipelines                  Pipeline[]
  stages                     PipelineStage[]
  associations               ObjectAssociation[]
  customProperties           CustomProperty[]
  importJobs                 ImportJob[]
  emailTemplates             EmailTemplate[]
  availabilityRules          AvailabilityRule[]
  meetingLinks               MeetingLink[]
  meetingBookings            MeetingBooking[]
  forms                      Form[]
  formSubmissions            FormSubmission[]
  conversations              Conversation[]
  auditLogs                  AuditLog[]
  savedViews                 SavedView[]
  businessUnits              BusinessUnit[]
  businessUnitMemberships    BusinessUnitMembership[]
  products                   Product[]
  businessUnitProducts       BusinessUnitProduct[]
  priceBookEntries           PriceBookEntry[]
  forecastCategories         ForecastCategory[]
  dealLineItems              DealLineItem[]
  dealParticipants           DealParticipant[]
  salesPerformanceEvents     SalesPerformanceEvent[]
  referrals                  Referral[]
  fieldVisits                FieldVisit[]
  metricDefinitions          MetricDefinition[]
  metricDefinitionVersions   MetricDefinitionVersion[]
  dailyMetricEntries         DailyMetricEntry[]
  metricValidationRules      MetricValidationRule[]
  kpiTargets                 KpiTarget[]
  businessCalendars          BusinessCalendar[]
  businessCalendarExceptions BusinessCalendarException[]
  actionPlans                ActionPlan[]
  legacySourceLinks          LegacySourceLink[]
  salesTerritories           SalesTerritory[]
  industries                 Industry[]
  outboundCampaigns          OutboundCampaign[]
  callLists                  CallList[]
}

model OrganizationMember {
  id                     String           @id @default(uuid()) @db.Uuid
  organizationId         String           @map("organization_id") @db.Uuid
  userId                 String           @map("user_id") @db.Uuid
  role                   OrganizationRole @default(USER)
  teamId                 String?          @map("team_id") @db.Uuid
  selectedBusinessUnitId String?          @map("selected_business_unit_id") @db.Uuid
  status                 MemberStatus     @default(ACTIVE)
  createdAt              DateTime         @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt              DateTime         @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization         Organization  @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  user                 User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  team                 Team?         @relation(fields: [teamId], references: [id], onDelete: SetNull)
  selectedBusinessUnit BusinessUnit? @relation("SelectedBusinessUnit", fields: [selectedBusinessUnitId], references: [id], onDelete: SetNull)

  @@unique([organizationId, userId])
  @@index([userId, status])
  @@index([organizationId, role, status])
  @@index([organizationId, selectedBusinessUnitId])
  @@map("organization_members")
}

model BusinessUnit {
  id                       String                    @id @default(uuid()) @db.Uuid
  organizationId           String                    @map("organization_id") @db.Uuid
  name                     String                    @db.VarChar(160)
  slug                     String                    @db.VarChar(80)
  description              String?
  status                   BusinessUnitStatus        @default(ACTIVE)
  displayOrder             Int                       @default(0) @map("display_order")
  amountMetricBasis        AmountMetricBasis?        @map("amount_metric_basis")
  confirmedAmountDateBasis ConfirmedAmountDateBasis? @map("confirmed_amount_date_basis")
  createdAt                DateTime                  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                DateTime                  @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization       Organization             @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  memberships        BusinessUnitMembership[]
  selectedByMembers  OrganizationMember[]     @relation("SelectedBusinessUnit")
  pipelines          Pipeline[]
  deals              Deal[]
  forms              Form[]
  products           BusinessUnitProduct[]
  forecastCategories ForecastCategory[]
  businessCalendars  BusinessCalendar[]

  @@unique([organizationId, slug])
  @@unique([organizationId, name])
  @@index([organizationId, status, displayOrder])
  @@map("business_units")
}

model BusinessUnitMembership {
  id             String             @id @default(uuid()) @db.Uuid
  organizationId String             @map("organization_id") @db.Uuid
  businessUnitId String             @map("business_unit_id") @db.Uuid
  userId         String             @map("user_id") @db.Uuid
  workFunction   WorkFunction       @map("work_function")
  isManager      Boolean            @default(false) @map("is_manager")
  status         BusinessUnitStatus @default(ACTIVE)
  createdAt      DateTime           @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime           @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnit BusinessUnit @relation(fields: [businessUnitId], references: [id], onDelete: Cascade)
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([businessUnitId, userId, workFunction])
  @@index([organizationId, userId, status])
  @@index([organizationId, businessUnitId, workFunction, status])
  @@map("business_unit_memberships")
}

model Team {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  name           String   @db.VarChar(120)
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization         @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  members      OrganizationMember[]

  @@unique([organizationId, name])
  @@index([organizationId])
  @@map("teams")
}

model Invitation {
  id             String           @id @default(uuid()) @db.Uuid
  organizationId String           @map("organization_id") @db.Uuid
  email          String           @db.VarChar(320)
  role           OrganizationRole @default(USER)
  tokenHash      String           @unique @map("token_hash") @db.VarChar(64)
  expiresAt      DateTime         @map("expires_at") @db.Timestamptz(3)
  acceptedAt     DateTime?        @map("accepted_at") @db.Timestamptz(3)
  createdAt      DateTime         @default(now()) @map("created_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@index([organizationId, email])
  @@index([expiresAt])
  @@map("invitations")
}

model AuthSession {
  id                   String   @id @default(uuid()) @db.Uuid
  tokenHash            String   @unique @map("token_hash") @db.VarChar(64)
  userId               String   @map("user_id") @db.Uuid
  activeOrganizationId String   @map("active_organization_id") @db.Uuid
  expiresAt            DateTime @map("expires_at") @db.Timestamptz(3)
  lastSeenAt           DateTime @default(now()) @map("last_seen_at") @db.Timestamptz(3)
  createdAt            DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  user               User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  activeOrganization Organization @relation(fields: [activeOrganizationId], references: [id], onDelete: Cascade)

  @@index([userId, expiresAt])
  @@index([activeOrganizationId])
  @@map("auth_sessions")
}

model Contact {
  id             String    @id @default(uuid()) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  ownerUserId    String?   @map("owner_user_id") @db.Uuid
  firstName      String?   @map("first_name") @db.VarChar(120)
  lastName       String?   @map("last_name") @db.VarChar(120)
  email          String?   @db.VarChar(320)
  phone          String?   @db.VarChar(40)
  mobilePhone    String?   @map("mobile_phone") @db.VarChar(40)
  jobTitle       String?   @map("job_title") @db.VarChar(120)
  lifecycleStage String?   @map("lifecycle_stage") @db.VarChar(80)
  leadStatus     String?   @map("lead_status") @db.VarChar(80)
  source         String?   @db.VarChar(120)
  memo           String?
  customFields   Json      @default("{}") @map("custom_fields") @db.JsonB
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime  @updatedAt @map("updated_at") @db.Timestamptz(3)
  deletedAt      DateTime? @map("deleted_at") @db.Timestamptz(3)

  organization    Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  owner           User?            @relation("ContactOwner", fields: [ownerUserId], references: [id], onDelete: SetNull)
  formSubmissions FormSubmission[]
  meetingBookings MeetingBooking[]
  conversations   Conversation[]

  @@unique([organizationId, email])
  @@index([organizationId, deletedAt, updatedAt])
  @@index([organizationId, ownerUserId])
  @@map("contacts")
}

model Company {
  id             String    @id @default(uuid()) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  ownerUserId    String?   @map("owner_user_id") @db.Uuid
  name           String    @db.VarChar(200)
  domain         String?   @db.VarChar(255)
  phone          String?   @db.VarChar(40)
  industry       String?   @db.VarChar(120)
  address        String?
  city           String?   @db.VarChar(120)
  prefecture     String?   @db.VarChar(120)
  postalCode     String?   @map("postal_code") @db.VarChar(20)
  websiteUrl     String?   @map("website_url")
  employeeCount  Int?      @map("employee_count")
  annualRevenue  Decimal?  @map("annual_revenue") @db.Decimal(18, 2)
  customFields   Json      @default("{}") @map("custom_fields") @db.JsonB
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime  @updatedAt @map("updated_at") @db.Timestamptz(3)
  deletedAt      DateTime? @map("deleted_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  owner        User?        @relation("CompanyOwner", fields: [ownerUserId], references: [id], onDelete: SetNull)

  @@unique([organizationId, domain])
  @@index([organizationId, deletedAt, updatedAt])
  @@index([organizationId, ownerUserId])
  @@map("companies")
}

model Pipeline {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  businessUnitId String?  @map("business_unit_id") @db.Uuid
  name           String   @db.VarChar(160)
  isDefault      Boolean  @default(false) @map("is_default")
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnit BusinessUnit?   @relation(fields: [businessUnitId], references: [id], onDelete: SetNull)
  stages       PipelineStage[]
  deals        Deal[]

  @@unique([organizationId, name])
  @@index([organizationId, businessUnitId, isDefault])
  @@map("pipelines")
}

model PipelineStage {
  id             String    @id @default(uuid()) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  pipelineId     String    @map("pipeline_id") @db.Uuid
  name           String    @db.VarChar(120)
  sortOrder      Int       @map("order")
  probability    Int       @default(0)
  stageType      StageType @default(OPEN) @map("stage_type")
  requiredFields Json      @default("[]") @map("required_fields") @db.JsonB
  staleDays      Int?      @map("stale_days")
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime  @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  pipeline     Pipeline     @relation(fields: [pipelineId], references: [id], onDelete: Cascade)
  deals        Deal[]

  @@unique([pipelineId, sortOrder])
  @@unique([pipelineId, name])
  @@index([organizationId])
  @@map("pipeline_stages")
}

model Deal {
  id                  String              @id @default(uuid()) @db.Uuid
  organizationId      String              @map("organization_id") @db.Uuid
  businessUnitId      String?             @map("business_unit_id") @db.Uuid
  ownerUserId         String?             @map("owner_user_id") @db.Uuid
  pipelineId          String              @map("pipeline_id") @db.Uuid
  stageId             String              @map("stage_id") @db.Uuid
  forecastCategoryId  String?             @map("forecast_category_id") @db.Uuid
  name                String              @db.VarChar(200)
  amount              Decimal?            @db.Decimal(18, 2)
  expectedCloseDate   DateTime?           @map("expected_close_date") @db.Date
  closeDate           DateTime?           @map("close_date") @db.Date
  probability         Int                 @default(0)
  status              DealStatus          @default(OPEN)
  decisionMakerStatus DecisionMakerStatus @default(UNKNOWN) @map("decision_maker_status")
  qualificationResult QualificationResult @default(UNDETERMINED) @map("qualification_result")
  lostReason          String?             @map("lost_reason")
  primaryLossReasonId String?             @map("primary_loss_reason_id") @db.Uuid
  lossReasonNote      String?             @map("loss_reason_note")
  lostByUserId        String?             @map("lost_by_user_id") @db.Uuid
  nextAction          String?             @map("next_action") @db.VarChar(240)
  nextActionDate      DateTime?           @map("next_action_date") @db.Date
  nextActionOwnerId   String?             @map("next_action_owner_id") @db.Uuid
  dealType            DealType            @default(NEW_BUSINESS) @map("deal_type")
  originProjectId     String?             @map("origin_project_id") @db.Uuid
  originDealId        String?             @map("origin_deal_id") @db.Uuid
  source              String?             @db.VarChar(120)
  externalId          String?             @map("external_id") @db.VarChar(160)
  legacyProgress      String?             @map("legacy_progress") @db.VarChar(160)
  wonAt               DateTime?           @map("won_at") @db.Timestamptz(3)
  lostAt              DateTime?           @map("lost_at") @db.Timestamptz(3)
  cancelledAt         DateTime?           @map("cancelled_at") @db.Timestamptz(3)
  invalidatedAt       DateTime?           @map("invalidated_at") @db.Timestamptz(3)
  customFields        Json                @default("{}") @map("custom_fields") @db.JsonB
  createdAt           DateTime            @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt           DateTime            @updatedAt @map("updated_at") @db.Timestamptz(3)
  deletedAt           DateTime?           @map("deleted_at") @db.Timestamptz(3)

  organization Organization      @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnit BusinessUnit?     @relation(fields: [businessUnitId], references: [id], onDelete: SetNull)
  owner        User?             @relation("DealOwner", fields: [ownerUserId], references: [id], onDelete: SetNull)
  pipeline     Pipeline          @relation(fields: [pipelineId], references: [id], onDelete: Restrict)
  stage        PipelineStage     @relation(fields: [stageId], references: [id], onDelete: Restrict)
  lineItems    DealLineItem[]
  participants DealParticipant[]

  @@unique([organizationId, externalId])
  @@index([organizationId, businessUnitId, pipelineId, stageId, deletedAt])
  @@index([organizationId, businessUnitId, status, closeDate])
  @@index([organizationId, forecastCategoryId])
  @@index([organizationId, ownerUserId])
  @@index([organizationId, dealType, originProjectId])
  @@map("deals")
}

model ObjectAssociation {
  id               String     @id @default(uuid()) @db.Uuid
  organizationId   String     @map("organization_id") @db.Uuid
  sourceObjectType ObjectType @map("source_object_type")
  sourceObjectId   String     @map("source_object_id") @db.Uuid
  targetObjectType ObjectType @map("target_object_type")
  targetObjectId   String     @map("target_object_id") @db.Uuid
  label            String?    @db.VarChar(80)
  isPrimary        Boolean    @default(false) @map("is_primary")
  createdAt        DateTime   @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt        DateTime   @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@unique([organizationId, sourceObjectType, sourceObjectId, targetObjectType, targetObjectId])
  @@index([organizationId, sourceObjectType, sourceObjectId])
  @@index([organizationId, targetObjectType, targetObjectId])
  @@map("object_associations")
}

model Activity {
  id             String       @id @default(uuid()) @db.Uuid
  organizationId String       @map("organization_id") @db.Uuid
  actorUserId    String?      @map("actor_user_id") @db.Uuid
  deliveryProjectId String?   @map("delivery_project_id") @db.Uuid
  type           ActivityType
  title          String       @db.VarChar(200)
  body           String?
  metadata       Json         @default("{}") @db.JsonB
  occurredAt     DateTime     @default(now()) @map("occurred_at") @db.Timestamptz(3)
  createdAt      DateTime     @default(now()) @map("created_at") @db.Timestamptz(3)
  deletedAt      DateTime?    @map("deleted_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  actor        User?        @relation(fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organizationId, occurredAt])
  @@index([organizationId, deliveryProjectId, occurredAt])
  @@map("activities")
}

model Task {
  id              String       @id @default(uuid()) @db.Uuid
  organizationId  String       @map("organization_id") @db.Uuid
  ownerUserId     String       @map("owner_user_id") @db.Uuid
  createdByUserId String       @map("created_by_user_id") @db.Uuid
  deliveryProjectId String?    @map("delivery_project_id") @db.Uuid
  sourceDeliveryStageId String? @map("source_delivery_stage_id") @db.Uuid
  autoTaskKey     String?      @map("auto_task_key") @db.VarChar(240)
  title           String       @db.VarChar(200)
  description     String?
  dueDate         DateTime?    @map("due_date") @db.Timestamptz(3)
  status          TaskStatus   @default(TODO)
  priority        TaskPriority @default(MEDIUM)
  taskType        TaskType     @default(OTHER) @map("task_type")
  completedAt     DateTime?    @map("completed_at") @db.Timestamptz(3)
  createdAt       DateTime     @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt       DateTime     @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  owner        User         @relation("TaskOwner", fields: [ownerUserId], references: [id], onDelete: Restrict)
  createdBy    User         @relation("TaskCreator", fields: [createdByUserId], references: [id], onDelete: Restrict)

  @@index([organizationId, ownerUserId, status, dueDate])
  @@index([organizationId, deliveryProjectId, status, dueDate])
  @@index([organizationId, deliveryProjectId, autoTaskKey])
  @@map("tasks")
}

model CustomProperty {
  id             String                   @id @default(uuid()) @db.Uuid
  organizationId String                   @map("organization_id") @db.Uuid
  businessUnitId String?                  @map("business_unit_id") @db.Uuid
  objectType     CustomPropertyObjectType @map("object_type")
  name           String                   @db.VarChar(80)
  label          String                   @db.VarChar(120)
  fieldType      CustomFieldType          @map("field_type")
  options        Json                     @default("[]") @db.JsonB
  isRequired     Boolean                  @default(false) @map("is_required")
  isUnique       Boolean                  @default(false) @map("is_unique")
  isSearchable   Boolean                  @default(false) @map("is_searchable")
  isFilterable   Boolean                  @default(false) @map("is_filterable")
  isReportable   Boolean                  @default(false) @map("is_reportable")
  sortOrder      Int                      @default(0) @map("order")
  createdAt      DateTime                 @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime                 @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@unique([organizationId, objectType, name])
  @@index([organizationId, businessUnitId, objectType, sortOrder])
  @@map("custom_properties")
}

model ImportJob {
  id               String       @id @default(uuid()) @db.Uuid
  organizationId   String       @map("organization_id") @db.Uuid
  uploadedByUserId String       @map("uploaded_by_user_id") @db.Uuid
  objectType       String       @map("object_type") @db.VarChar(80)
  status           ImportStatus @default(PENDING)
  totalRows        Int          @default(0) @map("total_rows")
  successCount     Int          @default(0) @map("success_count")
  errorCount       Int          @default(0) @map("error_count")
  skippedCount     Int          @default(0) @map("skipped_count")
  mapping          Json         @default("{}") @db.JsonB
  errorReport      Json         @default("[]") @map("error_report") @db.JsonB
  createdAt        DateTime     @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt        DateTime     @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  uploadedBy   User         @relation(fields: [uploadedByUserId], references: [id], onDelete: Restrict)

  @@index([organizationId, createdAt])
  @@map("import_jobs")
}

model SavedView {
  id             String                   @id @default(uuid()) @db.Uuid
  organizationId String                   @map("organization_id") @db.Uuid
  userId         String                   @map("user_id") @db.Uuid
  objectType     CustomPropertyObjectType @map("object_type")
  name           String                   @db.VarChar(120)
  filters        Json                     @default("{}") @db.JsonB
  columns        Json                     @default("[]") @db.JsonB
  sort           Json                     @default("{}") @db.JsonB
  isShared       Boolean                  @default(false) @map("is_shared")
  createdAt      DateTime                 @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime                 @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([organizationId, userId, objectType, name])
  @@index([organizationId, objectType, isShared])
  @@map("saved_views")
}

model EmailTemplate {
  id              String   @id @default(uuid()) @db.Uuid
  organizationId  String   @map("organization_id") @db.Uuid
  name            String   @db.VarChar(160)
  subject         String   @db.VarChar(200)
  body            String
  createdByUserId String   @map("created_by_user_id") @db.Uuid
  createdAt       DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt       DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  createdBy    User         @relation(fields: [createdByUserId], references: [id], onDelete: Restrict)

  @@unique([organizationId, name])
  @@map("email_templates")
}

model AvailabilityRule {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  userId         String   @map("user_id") @db.Uuid
  scheduleId     String?  @map("schedule_id") @db.Uuid
  weekday        Int
  startMinutes   Int      @map("start_minutes")
  endMinutes     Int      @map("end_minutes")
  timezone       String   @default("Asia/Tokyo") @db.VarChar(80)
  isEnabled      Boolean  @default(true) @map("is_enabled")
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([organizationId, userId, weekday])
  @@index([scheduleId, weekday])
  @@map("availability_rules")
}

model AvailabilitySchedule {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  userId         String   @map("user_id") @db.Uuid
  timezone       String   @default("Asia/Tokyo") @db.VarChar(80)
  name           String   @default("標準営業時間") @db.VarChar(160)
  isDefault      Boolean  @default(true) @map("is_default")
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([organizationId, userId, name])
  @@index([organizationId, userId, isDefault])
  @@map("availability_schedules")
}

model AvailabilityException {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  scheduleId     String   @map("schedule_id") @db.Uuid
  userId         String   @map("user_id") @db.Uuid
  date           DateTime @db.Date
  isAvailable    Boolean  @default(false) @map("is_available")
  startMinutes   Int?     @map("start_minutes")
  endMinutes     Int?     @map("end_minutes")
  reason         String?  @db.VarChar(240)
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([scheduleId, date])
  @@index([organizationId, userId, date])
  @@map("availability_exceptions")
}

model MeetingLink {
  id                             String                  @id @default(uuid()) @db.Uuid
  organizationId                 String                  @map("organization_id") @db.Uuid
  userId                         String                  @map("user_id") @db.Uuid
  ownerUserId                    String?                 @map("owner_user_id") @db.Uuid
  businessUnitId                 String?                 @map("business_unit_id") @db.Uuid
  teamId                         String?                 @map("team_id") @db.Uuid
  name                           String                  @db.VarChar(160)
  slug                           String                  @unique @db.VarChar(100)
  assignmentMode                 AssignmentMode          @default(FIXED_USER) @map("assignment_mode")
  workFunction                   WorkFunction?           @map("work_function")
  durationMinutes                Int                     @default(30) @map("duration_minutes")
  bufferBeforeMinutes            Int                     @default(0) @map("buffer_before_minutes")
  bufferAfterMinutes             Int                     @default(0) @map("buffer_after_minutes")
  minimumNoticeMinutes           Int                     @default(60) @map("minimum_notice_minutes")
  bookingHorizonDays             Int                     @default(14) @map("booking_horizon_days")
  timezone                       String                  @default("Asia/Tokyo") @db.VarChar(80)
  locationType                   MeetingLocationType     @default(GOOGLE_MEET) @map("location_type")
  locationValue                  String?                 @map("location_value") @db.VarChar(500)
  availableWeekdays              Json                    @default("[1,2,3,4,5]") @map("available_weekdays") @db.JsonB
  availableStartMinutes          Int                     @default(600) @map("available_start_minutes")
  availableEndMinutes            Int                     @default(1080) @map("available_end_minutes")
  slotIntervalMinutes            Int                     @default(30) @map("slot_interval_minutes")
  maxBookingsPerDay              Int?                    @map("max_bookings_per_day")
  cancellationDeadlineMinutes    Int?                    @map("cancellation_deadline_minutes")
  rescheduleDeadlineMinutes      Int?                    @map("reschedule_deadline_minutes")
  status                         MeetingLinkStatus       @default(ACTIVE)
  isActive                       Boolean                 @default(true) @map("is_active")
  googleCalendarEnabled          Boolean                 @default(false) @map("google_calendar_enabled")
  titleTemplate                  String?                 @map("title_template") @db.VarChar(240)
  holdMinutes                    Int                     @default(5) @map("hold_minutes")
  appointmentCreditPolicy        AppointmentCreditPolicy @default(ASSIGNED_USER) @map("appointment_credit_policy")
  appointmentCreditFixedUserId   String?                 @map("appointment_credit_fixed_user_id") @db.Uuid
  googleFallbackMode             String                  @default("crm_only") @map("google_fallback_mode") @db.VarChar(80)
  createdAt                      DateTime                @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                      DateTime                @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  user         User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  bookings     MeetingBooking[]

  @@index([organizationId, userId])
  @@index([organizationId, businessUnitId, status])
  @@map("meeting_links")
}

model MeetingBooking {
  id                          String               @id @default(uuid()) @db.Uuid
  organizationId              String               @map("organization_id") @db.Uuid
  meetingLinkId               String               @map("meeting_link_id") @db.Uuid
  contactId                   String?              @map("contact_id") @db.Uuid
  companyId                   String?              @map("company_id") @db.Uuid
  businessUnitId              String?              @map("business_unit_id") @db.Uuid
  dealId                      String?              @map("deal_id") @db.Uuid
  formSubmissionId            String?              @map("form_submission_id") @db.Uuid
  bookingHoldId               String?              @map("booking_hold_id") @db.Uuid
  externalSubmissionId        String?              @map("external_submission_id") @db.VarChar(240)
  setByUserId                 String?              @map("set_by_user_id") @db.Uuid
  hostUserId                  String?              @map("host_user_id") @db.Uuid
  assignedUserId              String?              @map("assigned_user_id") @db.Uuid
  creditedAppointmentSetterId String?              @map("credited_appointment_setter_id") @db.Uuid
  submittedByContactId        String?              @map("submitted_by_contact_id") @db.Uuid
  guestName                   String               @map("guest_name") @db.VarChar(120)
  guestEmail                  String               @map("guest_email") @db.VarChar(320)
  guestPhone                  String?              @map("guest_phone") @db.VarChar(40)
  startsAt                    DateTime             @map("starts_at") @db.Timestamptz(3)
  endsAt                      DateTime             @map("ends_at") @db.Timestamptz(3)
  status                      MeetingBookingStatus @default(SCHEDULED)
  bookingStatus               BookingStatus        @default(CONFIRMED) @map("booking_status")
  syncStatus                  CalendarSyncStatus   @default(NOT_REQUIRED) @map("sync_status")
  qualificationResult         QualificationResult  @default(UNDETERMINED) @map("qualification_result")
  appointmentSetAt            DateTime?            @map("appointment_set_at") @db.Timestamptz(3)
  attendedAt                  DateTime?            @map("attended_at") @db.Timestamptz(3)
  cancelledAt                 DateTime?            @map("cancelled_at") @db.Timestamptz(3)
  noShowAt                    DateTime?            @map("no_show_at") @db.Timestamptz(3)
  sourceChannel               String?              @map("source_channel") @db.VarChar(120)
  territoryId                 String?              @map("territory_id") @db.Uuid
  prefectureCode              String?              @map("prefecture_code") @db.VarChar(2)
  city                        String?              @db.VarChar(120)
  industryId                  String?              @map("industry_id") @db.Uuid
  productId                   String?              @map("product_id") @db.Uuid
  campaignId                  String?              @map("campaign_id") @db.Uuid
  callListId                  String?              @map("call_list_id") @db.Uuid
  meetingType                 String?              @map("meeting_type") @db.VarChar(120)
  timezone                    String               @default("Asia/Tokyo") @db.VarChar(80)
  idempotencyKey              String?              @map("idempotency_key") @db.VarChar(240)
  googleCalendarId            String?              @map("google_calendar_id") @db.VarChar(240)
  googleEventId               String?              @map("google_event_id") @db.VarChar(240)
  googleEventEtag             String?              @map("google_event_etag") @db.VarChar(240)
  googleEventHtmlLink         String?              @map("google_event_html_link")
  googleEventICalUid          String?              @map("google_event_ical_uid") @db.VarChar(240)
  lastSyncedAt                DateTime?            @map("last_synced_at") @db.Timestamptz(3)
  syncAttemptCount            Int                  @default(0) @map("sync_attempt_count")
  nextRetryAt                 DateTime?            @map("next_retry_at") @db.Timestamptz(3)
  syncErrorCode               String?              @map("sync_error_code") @db.VarChar(120)
  syncErrorMessage            String?              @map("sync_error_message")
  externalChangeType          CalendarExternalChangeType? @map("external_change_type")
  externalSyncStatus          CalendarSyncStatus?   @map("external_sync_status")
  externalChangeDetectedAt    DateTime?             @map("external_change_detected_at") @db.Timestamptz(3)
  externalChangeSnapshot      Json                  @default("{}") @map("external_change_snapshot") @db.JsonB
  bookingOrigin               BookingOrigin        @default(INTERNAL) @map("booking_origin")
  cancelReason                String?              @map("cancel_reason") @db.VarChar(500)
  legacyMetadata              Json                 @default("{}") @map("legacy_metadata") @db.JsonB
  createdAt                   DateTime             @default(now()) @map("created_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  meetingLink  MeetingLink  @relation(fields: [meetingLinkId], references: [id], onDelete: Cascade)
  contact      Contact?     @relation(fields: [contactId], references: [id], onDelete: SetNull)

  @@unique([meetingLinkId, startsAt])
  @@index([organizationId, startsAt])
  @@index([organizationId, businessUnitId, startsAt])
  @@index([organizationId, setByUserId, appointmentSetAt])
  @@index([organizationId, hostUserId, startsAt])
  @@index([organizationId, bookingStatus, syncStatus])
  @@index([organizationId, googleCalendarId, googleEventId])
  @@index([organizationId, meetingLinkId, externalSubmissionId])
  @@index([organizationId, dealId])
  @@index([organizationId, territoryId, startsAt])
  @@index([organizationId, prefectureCode, startsAt])
  @@index([organizationId, industryId, startsAt])
  @@index([organizationId, productId, startsAt])
  @@index([organizationId, campaignId, startsAt])
  @@index([organizationId, callListId, startsAt])
  @@map("meeting_bookings")
}

model Form {
  id                             String                  @id @default(uuid()) @db.Uuid
  organizationId                 String                  @map("organization_id") @db.Uuid
  businessUnitId                 String?                 @map("business_unit_id") @db.Uuid
  name                           String                  @db.VarChar(160)
  description                    String?
  slug                           String                  @unique @db.VarChar(100)
  status                         FormStatus              @default(DRAFT)
  formPurpose                    FormPurpose             @default(PUBLIC_INQUIRY) @map("form_purpose")
  isInternal                     Boolean                 @default(false) @map("is_internal")
  isDefaultForBusinessUnit       Boolean                 @default(false) @map("is_default_for_business_unit")
  fields                         Json                    @default("[]") @db.JsonB
  mappingSchema                  Json                    @default("{}") @map("mapping_schema") @db.JsonB
  routingConfig                  Json                    @default("{}") @map("routing_config") @db.JsonB
  schedulingConfig               Json                    @default("{}") @map("scheduling_config") @db.JsonB
  submitButtonText               String                  @default("送信する") @map("submit_button_text") @db.VarChar(80)
  completionMessage              String?                 @map("completion_message")
  redirectUrl                    String?                 @map("redirect_url")
  targetProductId                String?                 @map("target_product_id") @db.Uuid
  pipelineId                     String?                 @map("pipeline_id") @db.Uuid
  stageId                        String?                 @map("stage_id") @db.Uuid
  meetingLinkId                  String?                 @map("meeting_link_id") @db.Uuid
  assignmentMode                 AssignmentMode          @default(ROUND_ROBIN) @map("assignment_mode")
  fixedAssigneeUserId            String?                 @map("fixed_assignee_user_id") @db.Uuid
  teamId                         String?                 @map("team_id") @db.Uuid
  workFunction                   WorkFunction?           @default(IS) @map("work_function")
  appointmentCreditPolicy        AppointmentCreditPolicy @default(ASSIGNED_USER) @map("appointment_credit_policy")
  appointmentCreditFixedUserId   String?                 @map("appointment_credit_fixed_user_id") @db.Uuid
  publishedVersionId             String?                 @map("published_version_id") @db.Uuid
  privacyConsentVersion          String?                 @map("privacy_consent_version") @db.VarChar(80)
  googleFallbackMode             String                  @default("crm_only") @map("google_fallback_mode") @db.VarChar(80)
  createdAt                      DateTime                @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                      DateTime                @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnit BusinessUnit?    @relation(fields: [businessUnitId], references: [id], onDelete: SetNull)
  submissions  FormSubmission[]

  @@index([organizationId, businessUnitId, status])
  @@index([organizationId, businessUnitId, formPurpose, isDefaultForBusinessUnit])
  @@map("forms")
}

model FormVersion {
  id                       String            @id @default(uuid()) @db.Uuid
  organizationId           String            @map("organization_id") @db.Uuid
  businessUnitId           String?           @map("business_unit_id") @db.Uuid
  formId                   String            @map("form_id") @db.Uuid
  version                  Int
  status                   FormVersionStatus @default(DRAFT)
  nameSnapshot             String            @map("name_snapshot") @db.VarChar(160)
  descriptionSnapshot      String?           @map("description_snapshot")
  fieldSchema              Json              @map("field_schema") @db.JsonB
  mappingSchema            Json              @map("mapping_schema") @db.JsonB
  routingConfigSnapshot    Json              @map("routing_config_snapshot") @db.JsonB
  schedulingConfigSnapshot Json              @map("scheduling_config_snapshot") @db.JsonB
  submitButtonTextSnapshot String            @default("送信する") @map("submit_button_text_snapshot") @db.VarChar(80)
  completionMessageSnapshot String?          @map("completion_message_snapshot")
  publishedByUserId        String?           @map("published_by_user_id") @db.Uuid
  publishedAt              DateTime?         @map("published_at") @db.Timestamptz(3)
  createdAt                DateTime          @default(now()) @map("created_at") @db.Timestamptz(3)

  @@unique([formId, version])
  @@index([organizationId, businessUnitId, status])
  @@index([organizationId, formId, status])
  @@map("form_versions")
}

model FormSubmission {
  id                  String   @id @default(uuid()) @db.Uuid
  organizationId      String   @map("organization_id") @db.Uuid
  formId              String   @map("form_id") @db.Uuid
  formVersionId       String?  @map("form_version_id") @db.Uuid
  companyId           String?  @map("company_id") @db.Uuid
  contactId           String?  @map("contact_id") @db.Uuid
  dealId              String?  @map("deal_id") @db.Uuid
  meetingBookingId    String?  @map("meeting_booking_id") @db.Uuid
  routingRuleId       String?  @map("routing_rule_id") @db.Uuid
  assignedUserId      String?  @map("assigned_user_id") @db.Uuid
  idempotencyKey      String?  @map("idempotency_key") @db.VarChar(240)
  rawPayload          Json     @map("raw_payload") @db.JsonB
  normalizedPayload   Json     @default("{}") @map("normalized_payload") @db.JsonB
  duplicateCandidates Json     @default("[]") @map("duplicate_candidates") @db.JsonB
  routingResult       Json     @default("{}") @map("routing_result") @db.JsonB
  consentSnapshot     Json     @default("{}") @map("consent_snapshot") @db.JsonB
  ipAddress           String?  @map("ip_address") @db.VarChar(80)
  userAgent           String?  @map("user_agent") @db.VarChar(500)
  honeypotValue       String?  @map("honeypot_value") @db.VarChar(240)
  createdAt           DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  form         Form         @relation(fields: [formId], references: [id], onDelete: Cascade)
  contact      Contact?     @relation(fields: [contactId], references: [id], onDelete: SetNull)

  @@index([organizationId, formId, createdAt])
  @@index([organizationId, assignedUserId, createdAt])
  @@index([organizationId, dealId])
  @@unique([organizationId, idempotencyKey])
  @@map("form_submissions")
}

model RoutingRule {
  id             String               @id @default(uuid()) @db.Uuid
  organizationId String               @map("organization_id") @db.Uuid
  businessUnitId String?              @map("business_unit_id") @db.Uuid
  formId         String?              @map("form_id") @db.Uuid
  name           String               @db.VarChar(160)
  priority       Int                  @default(100)
  status         RoutingRuleStatus    @default(ACTIVE)
  conditionJoin  RoutingConditionJoin @default(AND) @map("condition_join")
  conditions     Json                 @default("[]") @db.JsonB
  actions        Json                 @default("[]") @db.JsonB
  stopProcessing Boolean              @default(true) @map("stop_processing")
  assignmentMode AssignmentMode?      @map("assignment_mode")
  fixedUserId    String?              @map("fixed_user_id") @db.Uuid
  teamId         String?              @map("team_id") @db.Uuid
  workFunction   WorkFunction?        @map("work_function")
  fallbackConfig Json                 @default("{}") @map("fallback_config") @db.JsonB
  version        Int                  @default(1)
  createdAt      DateTime             @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime             @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@index([organizationId, businessUnitId, formId, status, priority])
  @@map("routing_rules")
}

model RoutingExecutionLog {
  id               String   @id @default(uuid()) @db.Uuid
  organizationId   String   @map("organization_id") @db.Uuid
  routingRuleId    String?  @map("routing_rule_id") @db.Uuid
  formSubmissionId String?  @map("form_submission_id") @db.Uuid
  candidateUserIds Json     @default("[]") @map("candidate_user_ids") @db.JsonB
  selectedUserId   String?  @map("selected_user_id") @db.Uuid
  matched          Boolean  @default(false)
  reason           String?  @db.VarChar(500)
  fallbackReason   String?  @map("fallback_reason") @db.VarChar(500)
  inputSnapshot    Json     @default("{}") @map("input_snapshot") @db.JsonB
  resultSnapshot   Json     @default("{}") @map("result_snapshot") @db.JsonB
  executedAt       DateTime @default(now()) @map("executed_at") @db.Timestamptz(3)

  @@index([organizationId, formSubmissionId, executedAt])
  @@index([organizationId, routingRuleId, executedAt])
  @@map("routing_execution_logs")
}

model AssignmentCursor {
  id                 String   @id @default(uuid()) @db.Uuid
  organizationId     String   @map("organization_id") @db.Uuid
  scopeKey           String   @map("scope_key") @db.VarChar(240)
  lastAssignedUserId String?  @map("last_assigned_user_id") @db.Uuid
  position           Int      @default(0)
  updatedAt          DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([organizationId, scopeKey])
  @@map("assignment_cursors")
}

model BookingHold {
  id               String            @id @default(uuid()) @db.Uuid
  organizationId   String            @map("organization_id") @db.Uuid
  meetingLinkId    String            @map("meeting_link_id") @db.Uuid
  hostUserId       String?           @map("host_user_id") @db.Uuid
  scheduledStartAt DateTime          @map("scheduled_start_at") @db.Timestamptz(3)
  scheduledEndAt   DateTime          @map("scheduled_end_at") @db.Timestamptz(3)
  tokenHash        String            @map("token_hash") @db.VarChar(64)
  status           BookingHoldStatus @default(ACTIVE)
  expiresAt        DateTime          @map("expires_at") @db.Timestamptz(3)
  createdAt        DateTime          @default(now()) @map("created_at") @db.Timestamptz(3)

  @@unique([tokenHash])
  @@index([organizationId, meetingLinkId, scheduledStartAt])
  @@index([organizationId, hostUserId, scheduledStartAt, expiresAt])
  @@map("booking_holds")
}

model GoogleCalendarConnection {
  id                    String                         @id @default(uuid()) @db.Uuid
  organizationId        String                         @map("organization_id") @db.Uuid
  userId                String                         @map("user_id") @db.Uuid
  providerAccountId     String?                        @map("provider_account_id") @db.VarChar(240)
  googleEmail           String?                        @map("google_email") @db.VarChar(320)
  status                GoogleCalendarConnectionStatus @default(CONNECTED)
  encryptedAccessToken  String?                        @map("encrypted_access_token")
  encryptedRefreshToken String?                        @map("encrypted_refresh_token")
  encryptionKeyVersion  String                         @default("v1") @map("encryption_key_version") @db.VarChar(40)
  accessTokenExpiresAt  DateTime?                      @map("access_token_expires_at") @db.Timestamptz(3)
  grantedScopes         String[]                       @default([]) @map("granted_scopes")
  selectedWriteCalendarId String?                      @map("selected_write_calendar_id") @db.VarChar(240)
  selectedWriteCalendarName String?                    @map("selected_write_calendar_name") @db.VarChar(240)
  lastConnectedAt       DateTime?                      @map("last_connected_at") @db.Timestamptz(3)
  lastRefreshedAt       DateTime?                      @map("last_refreshed_at") @db.Timestamptz(3)
  lastErrorCode         String?                        @map("last_error_code") @db.VarChar(120)
  lastErrorMessage      String?                        @map("last_error_message")
  revokedAt             DateTime?                      @map("revoked_at") @db.Timestamptz(3)
  createdAt             DateTime                       @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt             DateTime                       @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([organizationId, userId])
  @@index([organizationId, status])
  @@map("google_calendar_connections")
}

model GoogleCalendarSelection {
  id                String   @id @default(uuid()) @db.Uuid
  connectionId      String   @map("connection_id") @db.Uuid
  googleCalendarId  String   @map("google_calendar_id") @db.VarChar(240)
  calendarName      String   @map("calendar_name") @db.VarChar(240)
  accessRole        String?  @map("access_role") @db.VarChar(80)
  isWriteCalendar   Boolean  @default(false) @map("is_write_calendar")
  useForBusyCheck   Boolean  @default(true) @map("use_for_busy_check")
  timezone          String?  @db.VarChar(80)
  syncToken         String?  @map("sync_token")
  syncTokenInvalidatedAt DateTime? @map("sync_token_invalidated_at") @db.Timestamptz(3)
  lastFullSyncAt    DateTime? @map("last_full_sync_at") @db.Timestamptz(3)
  lastIncrementalSyncAt DateTime? @map("last_incremental_sync_at") @db.Timestamptz(3)
  lastSyncStatus    CalendarSyncJobStatus? @map("last_sync_status")
  lastSyncErrorCode String?  @map("last_sync_error_code") @db.VarChar(120)
  lastSyncErrorMessage String? @map("last_sync_error_message")
  createdAt         DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([connectionId, googleCalendarId])
  @@index([connectionId, isWriteCalendar])
  @@map("google_calendar_selections")
}

model GoogleOAuthState {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  userId         String   @map("user_id") @db.Uuid
  stateHash      String   @unique @map("state_hash") @db.VarChar(64)
  codeVerifier   String?  @map("code_verifier") @db.VarChar(240)
  redirectPath   String?  @map("redirect_path") @db.VarChar(500)
  expiresAt      DateTime @map("expires_at") @db.Timestamptz(3)
  consumedAt     DateTime? @map("consumed_at") @db.Timestamptz(3)
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  @@index([organizationId, userId, expiresAt])
  @@map("google_oauth_states")
}

model GoogleCalendarWatchChannel {
  id                    String             @id @default(uuid()) @db.Uuid
  connectionId           String             @map("connection_id") @db.Uuid
  googleCalendarId       String             @map("google_calendar_id") @db.VarChar(240)
  channelId              String             @map("channel_id") @db.VarChar(240)
  resourceId             String?            @map("resource_id") @db.VarChar(240)
  encryptedChannelToken  String?            @map("encrypted_channel_token")
  syncToken              String?            @map("sync_token")
  status                 WatchChannelStatus @default(ACTIVE)
  expiresAt              DateTime?          @map("expires_at") @db.Timestamptz(3)
  lastMessageNumber      BigInt?            @map("last_message_number")
  notificationCount      Int                @default(0) @map("notification_count")
  lastRenewalAttemptAt   DateTime?          @map("last_renewal_attempt_at") @db.Timestamptz(3)
  lastRenewalError       String?            @map("last_renewal_error")
  lastNotificationAt     DateTime?          @map("last_notification_at") @db.Timestamptz(3)
  createdAt              DateTime           @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt              DateTime           @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([channelId])
  @@index([connectionId, googleCalendarId, status])
  @@map("google_calendar_watch_channels")
}

model CalendarSyncJob {
  id              String                @id @default(uuid()) @db.Uuid
  organizationId  String                @map("organization_id") @db.Uuid
  connectionId    String?               @map("connection_id") @db.Uuid
  selectionId     String?               @map("selection_id") @db.Uuid
  channelId       String?               @map("channel_id") @db.VarChar(240)
  jobType         CalendarSyncJobType   @map("job_type")
  status          CalendarSyncJobStatus @default(PENDING)
  correlationId   String                @map("correlation_id") @db.VarChar(120)
  startedAt       DateTime?             @map("started_at") @db.Timestamptz(3)
  finishedAt      DateTime?             @map("finished_at") @db.Timestamptz(3)
  processedCount  Int                   @default(0) @map("processed_count")
  errorCode       String?               @map("error_code") @db.VarChar(120)
  errorMessage    String?               @map("error_message")
  metadata        Json                  @default("{}") @db.JsonB
  createdAt       DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt       DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@index([organizationId, status, createdAt])
  @@index([connectionId, status])
  @@index([selectionId, status])
  @@map("calendar_sync_jobs")
}

model OperationalEvent {
  id               String               @id @default(uuid()) @db.Uuid
  organizationId   String               @map("organization_id") @db.Uuid
  eventType        OperationalEventType @map("event_type")
  correlationId    String?              @map("correlation_id") @db.VarChar(120)
  bookingId        String?              @map("booking_id") @db.Uuid
  formSubmissionId String?              @map("form_submission_id") @db.Uuid
  syncJobId        String?              @map("sync_job_id") @db.Uuid
  channelId        String?              @map("channel_id") @db.VarChar(240)
  status           String?              @db.VarChar(80)
  metadata         Json                 @default("{}") @db.JsonB
  occurredAt       DateTime             @default(now()) @map("occurred_at") @db.Timestamptz(3)

  @@index([organizationId, eventType, occurredAt])
  @@index([organizationId, bookingId])
  @@index([organizationId, formSubmissionId])
  @@index([organizationId, syncJobId])
  @@map("operational_events")
}

model Conversation {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  contactId      String?  @map("contact_id") @db.Uuid
  visitorName    String?  @map("visitor_name") @db.VarChar(120)
  visitorEmail   String?  @map("visitor_email") @db.VarChar(320)
  message        String
  metadata       Json     @default("{}") @db.JsonB
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  contact      Contact?     @relation(fields: [contactId], references: [id], onDelete: SetNull)

  @@index([organizationId, createdAt])
  @@map("conversations")
}

model AuditLog {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  actorUserId    String?  @map("actor_user_id") @db.Uuid
  action         String   @db.VarChar(120)
  targetType     String   @map("target_type") @db.VarChar(80)
  targetId       String?  @map("target_id") @db.Uuid
  before         Json?    @db.JsonB
  after          Json?    @db.JsonB
  ipAddress      String?  @map("ip_address") @db.VarChar(64)
  userAgent      String?  @map("user_agent")
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  actor        User?        @relation(fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organizationId, createdAt])
  @@index([organizationId, targetType, targetId])
  @@map("audit_logs")
}

model Product {
  id             String        @id @default(uuid()) @db.Uuid
  organizationId String        @map("organization_id") @db.Uuid
  name           String        @db.VarChar(160)
  normalizedName String        @map("normalized_name") @db.VarChar(160)
  sku            String?       @db.VarChar(80)
  description    String?
  category       String?       @db.VarChar(120)
  fulfillmentType FulfillmentType? @map("fulfillment_type")
  status         ProductStatus @default(ACTIVE)
  metadata       Json          @default("{}") @db.JsonB
  createdAt      DateTime      @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime      @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization         Organization          @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnitProducts BusinessUnitProduct[]
  priceBookEntries     PriceBookEntry[]
  lineItems            DealLineItem[]

  @@unique([organizationId, normalizedName])
  @@index([organizationId, status, name])
  @@map("products")
}

model BusinessUnitProduct {
  id             String        @id @default(uuid()) @db.Uuid
  organizationId String        @map("organization_id") @db.Uuid
  businessUnitId String        @map("business_unit_id") @db.Uuid
  productId      String        @map("product_id") @db.Uuid
  productKind    ProductKind?  @map("product_kind")
  fulfillmentType FulfillmentType? @map("fulfillment_type")
  autoCreateDeliveryProject Boolean @default(false) @map("auto_create_delivery_project")
  defaultDeliveryProjectTemplateId String? @map("default_delivery_project_template_id") @db.Uuid
  projectGroupingMode ProjectGroupingMode @default(GROUP_BY_DEAL) @map("project_grouping_mode")
  status         ProductStatus @default(ACTIVE)
  displayOrder   Int           @default(0) @map("display_order")
  metadata       Json          @default("{}") @db.JsonB
  createdAt      DateTime      @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime      @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnit BusinessUnit @relation(fields: [businessUnitId], references: [id], onDelete: Cascade)
  product      Product      @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@unique([organizationId, businessUnitId, productId])
  @@index([organizationId, businessUnitId, fulfillmentType, autoCreateDeliveryProject])
  @@index([organizationId, businessUnitId, productKind, status, displayOrder])
  @@map("business_unit_products")
}

model PriceBookEntry {
  id                String          @id @default(uuid()) @db.Uuid
  organizationId    String          @map("organization_id") @db.Uuid
  productId         String          @map("product_id") @db.Uuid
  businessUnitId    String?         @map("business_unit_id") @db.Uuid
  name              String          @db.VarChar(160)
  currency          String          @default("JPY") @db.VarChar(3)
  unitPriceAmount   Decimal?        @map("unit_price_amount") @db.Decimal(18, 2)
  initialFee        Decimal?        @map("initial_fee") @db.Decimal(18, 2)
  recurringFee      Decimal?        @map("recurring_fee") @db.Decimal(18, 2)
  revenueAmount     Decimal?        @map("revenue_amount") @db.Decimal(18, 2)
  grossProfitAmount Decimal?        @map("gross_profit_amount") @db.Decimal(18, 2)
  effectiveFrom     DateTime?       @map("effective_from") @db.Date
  effectiveUntil    DateTime?       @map("effective_until") @db.Date
  status            PriceBookStatus @default(ACTIVE)
  metadata          Json            @default("{}") @db.JsonB
  createdAt         DateTime        @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime        @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization   @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  product      Product        @relation(fields: [productId], references: [id], onDelete: Cascade)
  lineItems    DealLineItem[]

  @@index([organizationId, productId, status])
  @@index([organizationId, businessUnitId, status])
  @@map("price_book_entries")
}

model ForecastCategory {
  id             String                 @id @default(uuid()) @db.Uuid
  organizationId String                 @map("organization_id") @db.Uuid
  businessUnitId String?                @map("business_unit_id") @db.Uuid
  key            String                 @db.VarChar(80)
  name           String                 @db.VarChar(120)
  probability    Int                    @default(0)
  status         ForecastCategoryStatus @default(ACTIVE)
  displayOrder   Int                    @default(0) @map("display_order")
  isClosed       Boolean                @default(false) @map("is_closed")
  isOmitted      Boolean                @default(false) @map("is_omitted")
  legacyAliases  Json                   @default("[]") @map("legacy_aliases") @db.JsonB
  metadata       Json                   @default("{}") @db.JsonB
  createdAt      DateTime               @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime               @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization  @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnit BusinessUnit? @relation(fields: [businessUnitId], references: [id], onDelete: Cascade)

  @@unique([organizationId, businessUnitId, key])
  @@index([organizationId, businessUnitId, status, displayOrder])
  @@map("forecast_categories")
}

model DealLineItem {
  id                        String             @id @default(uuid()) @db.Uuid
  organizationId            String             @map("organization_id") @db.Uuid
  dealId                    String             @map("deal_id") @db.Uuid
  productId                 String?            @map("product_id") @db.Uuid
  priceBookEntryId          String?            @map("price_book_entry_id") @db.Uuid
  businessUnitId            String?            @map("business_unit_id") @db.Uuid
  name                      String             @db.VarChar(180)
  quantity                  Decimal            @default(1) @db.Decimal(18, 4)
  unitPriceAmount           Decimal?           @map("unit_price_amount") @db.Decimal(18, 2)
  initialFee                Decimal?           @map("initial_fee") @db.Decimal(18, 2)
  recurringFee              Decimal?           @map("recurring_fee") @db.Decimal(18, 2)
  revenueAmount             Decimal?           @map("revenue_amount") @db.Decimal(18, 2)
  grossProfitAmount         Decimal?           @map("gross_profit_amount") @db.Decimal(18, 2)
  expectedRevenueAmount     Decimal?           @map("expected_revenue_amount") @db.Decimal(18, 2)
  expectedGrossProfitAmount Decimal?           @map("expected_gross_profit_amount") @db.Decimal(18, 2)
  collectedAmount           Decimal?           @map("collected_amount") @db.Decimal(18, 2)
  contractedAt              DateTime?          @map("contracted_at") @db.Date
  collectedAt               DateTime?          @map("collected_at") @db.Date
  billingStartedAt          DateTime?          @map("billing_started_at") @db.Date
  cancelledAt               DateTime?          @map("cancelled_at") @db.Date
  status                    DealLineItemStatus @default(PROPOSED)
  lossReasonId              String?            @map("loss_reason_id") @db.Uuid
  lossReasonNote            String?            @map("loss_reason_note")
  lostAt                    DateTime?          @map("lost_at") @db.Timestamptz(3)
  source                    String?            @db.VarChar(120)
  customFields              Json               @default("{}") @map("custom_fields") @db.JsonB
  metadata                  Json               @default("{}") @db.JsonB
  createdAt                 DateTime           @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                 DateTime           @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization   Organization    @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  deal           Deal            @relation(fields: [dealId], references: [id], onDelete: Cascade)
  product        Product?        @relation(fields: [productId], references: [id], onDelete: SetNull)
  priceBookEntry PriceBookEntry? @relation(fields: [priceBookEntryId], references: [id], onDelete: SetNull)

  @@index([organizationId, businessUnitId, status])
  @@index([organizationId, dealId])
  @@index([organizationId, productId])
  @@index([organizationId, lossReasonId])
  @@index([organizationId, contractedAt])
  @@index([organizationId, collectedAt])
  @@index([organizationId, billingStartedAt])
  @@map("deal_line_items")
}

model DealParticipant {
  id                 String                @id @default(uuid()) @db.Uuid
  organizationId     String                @map("organization_id") @db.Uuid
  dealId             String                @map("deal_id") @db.Uuid
  userId             String?               @map("user_id") @db.Uuid
  workFunction       WorkFunction?         @map("work_function")
  role               DealParticipantRole
  status             DealParticipantStatus @default(ACTIVE)
  contributionWeight Decimal               @default(1) @map("contribution_weight") @db.Decimal(5, 2)
  creditShare        Decimal?              @map("credit_share") @db.Decimal(5, 2)
  creditedAt         DateTime?             @map("credited_at") @db.Timestamptz(3)
  snapshotUserName   String?               @map("snapshot_user_name") @db.VarChar(120)
  metadata           Json                  @default("{}") @db.JsonB
  createdAt          DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt          DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  deal         Deal         @relation(fields: [dealId], references: [id], onDelete: Cascade)

  @@index([organizationId, dealId, role])
  @@index([organizationId, userId, role, creditedAt])
  @@map("deal_participants")
}

model SalesPerformanceEvent {
  id                 String                      @id @default(uuid()) @db.Uuid
  organizationId     String                      @map("organization_id") @db.Uuid
  businessUnitId     String?                     @map("business_unit_id") @db.Uuid
  dealId             String?                     @map("deal_id") @db.Uuid
  dealLineItemId     String?                     @map("deal_line_item_id") @db.Uuid
  meetingBookingId   String?                     @map("meeting_booking_id") @db.Uuid
  referralId         String?                     @map("referral_id") @db.Uuid
  fieldVisitId       String?                     @map("field_visit_id") @db.Uuid
  metricDefinitionId String?                     @map("metric_definition_id") @db.Uuid
  creditedUserId     String?                     @map("credited_user_id") @db.Uuid
  creditedRole       DealParticipantRole?        @map("credited_role")
  workFunction       WorkFunction?               @map("work_function")
  eventType          SalesPerformanceEventType   @map("event_type")
  source             SalesPerformanceEventSource @default(SYSTEM)
  occurredAt         DateTime                    @map("occurred_at") @db.Timestamptz(3)
  quantity           Decimal                     @default(1) @db.Decimal(18, 4)
  amount             Decimal?                    @db.Decimal(18, 2)
  territoryId        String?                     @map("territory_id") @db.Uuid
  prefectureCode     String?                     @map("prefecture_code") @db.VarChar(2)
  city               String?                     @db.VarChar(120)
  industryId         String?                     @map("industry_id") @db.Uuid
  productId          String?                     @map("product_id") @db.Uuid
  campaignId         String?                     @map("campaign_id") @db.Uuid
  callListId         String?                     @map("call_list_id") @db.Uuid
  idempotencyKey     String                      @map("idempotency_key") @db.VarChar(240)
  metadata           Json                        @default("{}") @db.JsonB
  createdAt          DateTime                    @default(now()) @map("created_at") @db.Timestamptz(3)
  cancelledAt        DateTime?                   @map("cancelled_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@unique([organizationId, idempotencyKey])
  @@index([organizationId, businessUnitId, occurredAt])
  @@index([organizationId, eventType, occurredAt])
  @@index([organizationId, creditedUserId, occurredAt])
  @@index([organizationId, dealId])
  @@index([organizationId, territoryId, occurredAt])
  @@index([organizationId, prefectureCode, occurredAt])
  @@index([organizationId, industryId, occurredAt])
  @@index([organizationId, productId, occurredAt])
  @@index([organizationId, campaignId, occurredAt])
  @@index([organizationId, callListId, occurredAt])
  @@map("sales_performance_events")
}

model SalesTerritory {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  businessUnitId String?  @map("business_unit_id") @db.Uuid
  name           String   @db.VarChar(160)
  description    String?
  mappingRules   Json     @default("[]") @map("mapping_rules") @db.JsonB
  isActive       Boolean  @default(true) @map("is_active")
  displayOrder   Int      @default(0) @map("display_order")
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@unique([organizationId, businessUnitId, name])
  @@index([organizationId, businessUnitId, isActive, displayOrder])
  @@map("sales_territories")
}

model Industry {
  id             String    @id @default(uuid()) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  parentId       String?   @map("parent_id") @db.Uuid
  code           String    @db.VarChar(80)
  name           String    @db.VarChar(160)
  isActive       Boolean   @default(true) @map("is_active")
  displayOrder   Int       @default(0) @map("display_order")
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime  @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  parent       Industry?    @relation("IndustryHierarchy", fields: [parentId], references: [id], onDelete: SetNull)
  children     Industry[]   @relation("IndustryHierarchy")

  @@unique([organizationId, code])
  @@index([organizationId, parentId, isActive, displayOrder])
  @@map("industries")
}

model OutboundCampaign {
  id             String    @id @default(uuid()) @db.Uuid
  organizationId String    @map("organization_id") @db.Uuid
  businessUnitId String?   @map("business_unit_id") @db.Uuid
  name           String    @db.VarChar(160)
  productId      String?   @map("product_id") @db.Uuid
  territoryId    String?   @map("territory_id") @db.Uuid
  prefectureCode String?   @map("prefecture_code") @db.VarChar(2)
  industryId     String?   @map("industry_id") @db.Uuid
  status         String    @default("ACTIVE") @db.VarChar(40)
  startDate      DateTime? @map("start_date") @db.Date
  endDate        DateTime? @map("end_date") @db.Date
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime  @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@unique([organizationId, businessUnitId, name])
  @@index([organizationId, businessUnitId, status])
  @@index([organizationId, territoryId])
  @@index([organizationId, industryId])
  @@map("outbound_campaigns")
}

model CallList {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  businessUnitId String?  @map("business_unit_id") @db.Uuid
  campaignId     String?  @map("campaign_id") @db.Uuid
  name           String   @db.VarChar(160)
  territoryId    String?  @map("territory_id") @db.Uuid
  prefectureCode String?  @map("prefecture_code") @db.VarChar(2)
  industryId     String?  @map("industry_id") @db.Uuid
  productId      String?  @map("product_id") @db.Uuid
  recordCount    Int      @default(0) @map("record_count")
  status         String   @default("ACTIVE") @db.VarChar(40)
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@unique([organizationId, businessUnitId, name])
  @@index([organizationId, businessUnitId, status])
  @@index([organizationId, campaignId])
  @@index([organizationId, territoryId])
  @@index([organizationId, industryId])
  @@map("call_lists")
}

model Referral {
  id                  String         @id @default(uuid()) @db.Uuid
  organizationId      String         @map("organization_id") @db.Uuid
  businessUnitId      String?        @map("business_unit_id") @db.Uuid
  dealId              String?        @map("deal_id") @db.Uuid
  referrerUserId      String?        @map("referrer_user_id") @db.Uuid
  ownerUserId         String?        @map("owner_user_id") @db.Uuid
  referredCompanyName String         @map("referred_company_name") @db.VarChar(200)
  referredContactName String?        @map("referred_contact_name") @db.VarChar(160)
  referredEmail       String?        @map("referred_email") @db.VarChar(320)
  referredPhone       String?        @map("referred_phone") @db.VarChar(40)
  status              ReferralStatus @default(NEW)
  referredAt          DateTime       @default(now()) @map("referred_at") @db.Timestamptz(3)
  appointmentSetAt    DateTime?      @map("appointment_set_at") @db.Timestamptz(3)
  meetingAttendedAt   DateTime?      @map("meeting_attended_at") @db.Timestamptz(3)
  wonAt               DateTime?      @map("won_at") @db.Timestamptz(3)
  metadata            Json           @default("{}") @db.JsonB
  createdAt           DateTime       @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt           DateTime       @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@index([organizationId, businessUnitId, referredAt])
  @@index([organizationId, referrerUserId, referredAt])
  @@index([organizationId, status])
  @@map("referrals")
}

model FieldVisit {
  id               String           @id @default(uuid()) @db.Uuid
  organizationId   String           @map("organization_id") @db.Uuid
  businessUnitId   String?          @map("business_unit_id") @db.Uuid
  ownerUserId      String?          @map("owner_user_id") @db.Uuid
  dealId           String?          @map("deal_id") @db.Uuid
  companyName      String           @map("company_name") @db.VarChar(200)
  contactName      String?          @map("contact_name") @db.VarChar(160)
  address          String?
  status           FieldVisitStatus @default(VISITED)
  visitedAt        DateTime         @map("visited_at") @db.Timestamptz(3)
  connectedAt      DateTime?        @map("connected_at") @db.Timestamptz(3)
  ownerConnectedAt DateTime?        @map("owner_connected_at") @db.Timestamptz(3)
  meetingSetAt     DateTime?        @map("meeting_set_at") @db.Timestamptz(3)
  appointmentSetAt DateTime?        @map("appointment_set_at") @db.Timestamptz(3)
  wonAt            DateTime?        @map("won_at") @db.Timestamptz(3)
  sameDayWon       Boolean          @default(false) @map("same_day_won")
  metadata         Json             @default("{}") @db.JsonB
  createdAt        DateTime         @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt        DateTime         @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@index([organizationId, businessUnitId, visitedAt])
  @@index([organizationId, ownerUserId, visitedAt])
  @@index([organizationId, status])
  @@map("field_visits")
}

model MetricDefinition {
  id                  String               @id @default(uuid()) @db.Uuid
  organizationId      String               @map("organization_id") @db.Uuid
  businessUnitId      String?              @map("business_unit_id") @db.Uuid
  key                 String               @db.VarChar(120)
  displayName         String               @map("display_name") @db.VarChar(160)
  description         String?
  category            MetricCategory       @default(ACTIVITY)
  unit                MetricUnit           @default(COUNT)
  sourceType          MetricSourceType     @map("source_type")
  aggregation         MetricAggregation    @default(SUM)
  workFunction        WorkFunction?        @map("work_function")
  objectType          String?              @map("object_type") @db.VarChar(80)
  dateField           String?              @map("date_field") @db.VarChar(80)
  attributionRole     DealParticipantRole? @map("attribution_role")
  numeratorMetricId   String?              @map("numerator_metric_id") @db.Uuid
  denominatorMetricId String?              @map("denominator_metric_id") @db.Uuid
  isActive            Boolean              @default(true) @map("is_active")
  isPrimary           Boolean              @default(false) @map("is_primary")
  isVisibleByDefault  Boolean              @default(true) @map("is_visible_by_default")
  displayOrder        Int                  @default(0) @map("display_order")
  minSampleSize       Int                  @default(0) @map("min_sample_size")
  queryDefinition     Json                 @default("{}") @map("query_definition") @db.JsonB
  filterDefinition    Json                 @default("{}") @map("filter_definition") @db.JsonB
  metadata            Json                 @default("{}") @db.JsonB
  createdAt           DateTime             @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt           DateTime             @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization              @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  versions     MetricDefinitionVersion[]
  dailyEntries DailyMetricEntry[]
  rules        MetricValidationRule[]
  targets      KpiTarget[]
  actionPlans  ActionPlan[]

  @@unique([organizationId, key])
  @@index([organizationId, businessUnitId, workFunction, isActive])
  @@index([organizationId, sourceType, aggregation])
  @@map("metric_definitions")
}

model MetricDefinitionVersion {
  id                 String            @id @default(uuid()) @db.Uuid
  organizationId     String            @map("organization_id") @db.Uuid
  metricDefinitionId String            @map("metric_definition_id") @db.Uuid
  version            Int
  displayName        String            @map("display_name") @db.VarChar(160)
  description        String?
  sourceType         MetricSourceType  @map("source_type")
  aggregation        MetricAggregation
  unit               MetricUnit
  queryDefinition    Json              @default("{}") @map("query_definition") @db.JsonB
  filterDefinition   Json              @default("{}") @map("filter_definition") @db.JsonB
  createdByUserId    String?           @map("created_by_user_id") @db.Uuid
  isCurrent          Boolean           @default(true) @map("is_current")
  createdAt          DateTime          @default(now()) @map("created_at") @db.Timestamptz(3)

  organization     Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  metricDefinition MetricDefinition @relation(fields: [metricDefinitionId], references: [id], onDelete: Cascade)

  @@unique([metricDefinitionId, version])
  @@index([organizationId, metricDefinitionId, isCurrent])
  @@map("metric_definition_versions")
}

model DailyMetricEntry {
  id                 String            @id @default(uuid()) @db.Uuid
  organizationId     String            @map("organization_id") @db.Uuid
  businessUnitId     String?           @map("business_unit_id") @db.Uuid
  metricDefinitionId String            @map("metric_definition_id") @db.Uuid
  userId             String            @map("user_id") @db.Uuid
  workFunction       WorkFunction?     @map("work_function")
  targetDate         DateTime          @map("target_date") @db.Date
  value              Decimal           @db.Decimal(18, 4)
  source             DailyMetricSource @default(MANUAL)
  status             DailyMetricStatus @default(DRAFT)
  submittedAt        DateTime?         @map("submitted_at") @db.Timestamptz(3)
  approvedAt         DateTime?         @map("approved_at") @db.Timestamptz(3)
  approvedByUserId   String?           @map("approved_by_user_id") @db.Uuid
  lockedAt           DateTime?         @map("locked_at") @db.Timestamptz(3)
  comment            String?
  dimensions         Json              @default("{}") @db.JsonB
  dimensionHash      String            @default("default") @map("dimension_hash") @db.VarChar(80)
  metadata           Json              @default("{}") @db.JsonB
  createdAt          DateTime          @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt          DateTime          @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization     Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  metricDefinition MetricDefinition @relation(fields: [metricDefinitionId], references: [id], onDelete: Cascade)

  @@index([organizationId, businessUnitId, targetDate])
  @@index([organizationId, metricDefinitionId, targetDate])
  @@index([organizationId, userId, targetDate])
  @@unique([organizationId, businessUnitId, userId, workFunction, metricDefinitionId, targetDate, source, dimensionHash])
  @@map("daily_metric_entries")
}

model MetricValidationRule {
  id                 String             @id @default(uuid()) @db.Uuid
  organizationId     String             @map("organization_id") @db.Uuid
  metricDefinitionId String             @map("metric_definition_id") @db.Uuid
  key                String             @db.VarChar(120)
  name               String             @db.VarChar(160)
  severity           ValidationSeverity @default(WARNING)
  condition          Json               @default("{}") @db.JsonB
  message            String
  isActive           Boolean            @default(true) @map("is_active")
  createdAt          DateTime           @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt          DateTime           @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization     Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  metricDefinition MetricDefinition @relation(fields: [metricDefinitionId], references: [id], onDelete: Cascade)

  @@unique([metricDefinitionId, key])
  @@index([organizationId, isActive])
  @@map("metric_validation_rules")
}

model KpiTarget {
  id                 String           @id @default(uuid()) @db.Uuid
  organizationId     String           @map("organization_id") @db.Uuid
  metricDefinitionId String           @map("metric_definition_id") @db.Uuid
  businessUnitId     String?          @map("business_unit_id") @db.Uuid
  userId             String?          @map("user_id") @db.Uuid
  teamId             String?          @map("team_id") @db.Uuid
  workFunction       WorkFunction?    @map("work_function")
  scopeKey           String           @map("scope_key") @db.VarChar(240)
  periodType         MetricPeriodType @default(MONTHLY) @map("period_type")
  periodStart        DateTime         @map("period_start") @db.Date
  periodEnd          DateTime         @map("period_end") @db.Date
  targetValue        Decimal          @map("target_value") @db.Decimal(18, 4)
  metadata           Json             @default("{}") @db.JsonB
  createdAt          DateTime         @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt          DateTime         @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization     Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  metricDefinition MetricDefinition @relation(fields: [metricDefinitionId], references: [id], onDelete: Cascade)
  actionPlans      ActionPlan[]

  @@unique([organizationId, metricDefinitionId, scopeKey, periodStart, periodEnd])
  @@index([organizationId, businessUnitId, periodStart, periodEnd])
  @@index([organizationId, userId, periodStart, periodEnd])
  @@map("kpi_targets")
}

model BusinessCalendar {
  id                 String   @id @default(uuid()) @db.Uuid
  organizationId     String   @map("organization_id") @db.Uuid
  businessUnitId     String?  @map("business_unit_id") @db.Uuid
  name               String   @db.VarChar(160)
  timezone           String   @default("Asia/Tokyo") @db.VarChar(80)
  workWeekDefinition Json     @default("{}") @map("work_week_definition") @db.JsonB
  defaultHolidays    Json     @default("[]") @map("default_holidays") @db.JsonB
  isDefault          Boolean  @default(false) @map("is_default")
  createdAt          DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt          DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization                @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  businessUnit BusinessUnit?               @relation(fields: [businessUnitId], references: [id], onDelete: Cascade)
  exceptions   BusinessCalendarException[]

  @@unique([organizationId, businessUnitId, name])
  @@index([organizationId, businessUnitId, isDefault])
  @@map("business_calendars")
}

model BusinessCalendarException {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  calendarId     String   @map("calendar_id") @db.Uuid
  targetDate     DateTime @map("target_date") @db.Date
  isWorkingDay   Boolean  @map("is_working_day")
  name           String?  @db.VarChar(160)
  metadata       Json     @default("{}") @db.JsonB
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization Organization     @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  calendar     BusinessCalendar @relation(fields: [calendarId], references: [id], onDelete: Cascade)

  @@unique([calendarId, targetDate])
  @@index([organizationId, targetDate])
  @@map("business_calendar_exceptions")
}

model ActionPlan {
  id                 String             @id @default(uuid()) @db.Uuid
  organizationId     String             @map("organization_id") @db.Uuid
  businessUnitId     String?            @map("business_unit_id") @db.Uuid
  workFunction       WorkFunction?      @map("work_function")
  ownerUserId        String?            @map("owner_user_id") @db.Uuid
  targetId           String?            @map("target_id") @db.Uuid
  metricDefinitionId String?            @map("metric_definition_id") @db.Uuid
  title              String             @db.VarChar(200)
  description        String?
  dueDate            DateTime?          @map("due_date") @db.Date
  status             ActionPlanStatus   @default(NOT_STARTED)
  priority           ActionPlanPriority @default(MEDIUM)
  completedAt        DateTime?          @map("completed_at") @db.Timestamptz(3)
  createdByUserId    String?            @map("created_by_user_id") @db.Uuid
  metadata           Json               @default("{}") @db.JsonB
  createdAt          DateTime           @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt          DateTime           @updatedAt @map("updated_at") @db.Timestamptz(3)

  organization     Organization      @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  metricDefinition MetricDefinition? @relation(fields: [metricDefinitionId], references: [id], onDelete: SetNull)
  target           KpiTarget?        @relation(fields: [targetId], references: [id], onDelete: SetNull)

  @@index([organizationId, businessUnitId, status, dueDate])
  @@index([organizationId, ownerUserId, status, dueDate])
  @@index([organizationId, metricDefinitionId])
  @@map("action_plans")
}

model LegacySourceLink {
  id                  String   @id @default(uuid()) @db.Uuid
  organizationId      String   @map("organization_id") @db.Uuid
  importJobId         String?  @map("import_job_id") @db.Uuid
  provider            String   @db.VarChar(80)
  workbookFingerprint String   @map("workbook_fingerprint") @db.VarChar(160)
  sheetName           String   @map("sheet_name") @db.VarChar(160)
  rowNumber           Int      @map("row_number")
  rowFingerprint      String   @map("row_fingerprint") @db.VarChar(160)
  targetObjectType    String   @map("target_object_type") @db.VarChar(80)
  targetObjectId      String   @map("target_object_id") @db.VarChar(120)
  importedAt          DateTime @default(now()) @map("imported_at") @db.Timestamptz(3)
  metadata            Json     @default("{}") @db.JsonB

  organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  @@unique([organizationId, provider, workbookFingerprint, sheetName, rowNumber, rowFingerprint, targetObjectType])
  @@index([organizationId, targetObjectType, targetObjectId])
  @@index([organizationId, workbookFingerprint, sheetName])
  @@map("legacy_source_links")
}

model CustomPropertyProductScope {
  id               String   @id @default(uuid()) @db.Uuid
  organizationId   String   @map("organization_id") @db.Uuid
  customPropertyId String   @map("custom_property_id") @db.Uuid
  productId        String   @map("product_id") @db.Uuid
  createdAt        DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  @@unique([customPropertyId, productId])
  @@index([organizationId, productId])
  @@map("custom_property_product_scopes")
}

model ProductAttachmentRule {
  id                String                    @id @default(uuid()) @db.Uuid
  organizationId    String                    @map("organization_id") @db.Uuid
  businessUnitId    String?                   @map("business_unit_id") @db.Uuid
  name              String                    @db.VarChar(160)
  attachedProductId String                    @map("attached_product_id") @db.Uuid
  denominatorMode   AttachmentDenominatorMode @map("denominator_mode")
  dateBasis         ConfirmedAmountDateBasis? @map("date_basis")
  targetRate        Decimal?                  @map("target_rate") @db.Decimal(8, 4)
  eligibilityFilter Json                      @default("{}") @map("eligibility_filter") @db.JsonB
  isActive          Boolean                   @default(true) @map("is_active")
  displayOrder      Int                       @default(0) @map("display_order")
  createdAt         DateTime                  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime                  @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@index([organizationId, businessUnitId, isActive, displayOrder])
  @@index([organizationId, attachedProductId])
  @@map("product_attachment_rules")
}

model ProductAttachmentRuleBaseProduct {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  ruleId         String   @map("rule_id") @db.Uuid
  productId      String   @map("product_id") @db.Uuid
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  @@unique([ruleId, productId])
  @@index([organizationId, productId])
  @@map("product_attachment_rule_base_products")
}

model LossReasonDefinition {
  id               String             @id @default(uuid()) @db.Uuid
  organizationId   String             @map("organization_id") @db.Uuid
  businessUnitId   String?            @map("business_unit_id") @db.Uuid
  productId        String?            @map("product_id") @db.Uuid
  code             String             @db.VarChar(80)
  name             String             @db.VarChar(160)
  category         String?            @db.VarChar(120)
  applicableScope  LossReasonScope    @default(BOTH) @map("applicable_scope")
  applicableStatus LossReasonStatus[] @default([]) @map("applicable_status")
  requiresNote     Boolean            @default(false) @map("requires_note")
  isActive         Boolean            @default(true) @map("is_active")
  displayOrder     Int                @default(0) @map("display_order")
  createdAt        DateTime           @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt        DateTime           @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([organizationId, businessUnitId, productId, code])
  @@index([organizationId, businessUnitId, productId, isActive, displayOrder])
  @@map("loss_reason_definitions")
}

model DealAlertRule {
  id             String            @id @default(uuid()) @db.Uuid
  organizationId String            @map("organization_id") @db.Uuid
  businessUnitId String?           @map("business_unit_id") @db.Uuid
  pipelineId     String?           @map("pipeline_id") @db.Uuid
  stageId        String?           @map("stage_id") @db.Uuid
  type           DealAlertRuleType
  name           String            @db.VarChar(160)
  thresholdDays  Int?              @map("threshold_days")
  config         Json              @default("{}") @db.JsonB
  isActive       Boolean           @default(true) @map("is_active")
  createdAt      DateTime          @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime          @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@index([organizationId, businessUnitId, pipelineId, stageId, isActive])
  @@map("deal_alert_rules")
}

model DeliveryPipeline {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  businessUnitId String?  @map("business_unit_id") @db.Uuid
  name           String   @db.VarChar(160)
  isDefault      Boolean  @default(false) @map("is_default")
  isActive       Boolean  @default(true) @map("is_active")
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  stages DeliveryPipelineStage[]

  @@unique([organizationId, businessUnitId, name])
  @@index([organizationId, businessUnitId, isDefault, isActive])
  @@map("delivery_pipelines")
}

model DeliveryPipelineStage {
  id             String            @id @default(uuid()) @db.Uuid
  organizationId String            @map("organization_id") @db.Uuid
  businessUnitId String?           @map("business_unit_id") @db.Uuid
  pipelineId     String            @map("pipeline_id") @db.Uuid
  name           String            @db.VarChar(120)
  sortOrder      Int               @map("order")
  color          String?           @db.VarChar(40)
  stageType      DeliveryStageType @default(NORMAL) @map("stage_type")
  staleDays      Int?              @map("stale_days")
  requiredFields Json              @default("[]") @map("required_fields") @db.JsonB
  taskTemplates  Json              @default("[]") @map("task_templates") @db.JsonB
  isCompleted    Boolean           @default(false) @map("is_completed")
  isPaused       Boolean           @default(false) @map("is_paused")
  createdAt      DateTime          @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt      DateTime          @updatedAt @map("updated_at") @db.Timestamptz(3)

  pipeline DeliveryPipeline @relation(fields: [pipelineId], references: [id], onDelete: Cascade)

  @@unique([pipelineId, sortOrder])
  @@unique([pipelineId, name])
  @@index([organizationId, businessUnitId, pipelineId, sortOrder])
  @@map("delivery_pipeline_stages")
}

model DeliveryProjectTemplate {
  id                         String   @id @default(uuid()) @db.Uuid
  organizationId             String   @map("organization_id") @db.Uuid
  businessUnitId             String?  @map("business_unit_id") @db.Uuid
  name                       String   @db.VarChar(160)
  description                String?
  pipelineId                 String?  @map("pipeline_id") @db.Uuid
  defaultCsTeamId            String?  @map("default_cs_team_id") @db.Uuid
  defaultCsUserId            String?  @map("default_cs_user_id") @db.Uuid
  defaultDueBusinessDays     Int?     @map("default_due_business_days")
  autoCreate                 Boolean  @default(false) @map("auto_create")
  handoffRequiredFields      Json     @default("[]") @map("handoff_required_fields") @db.JsonB
  defaultScope               Json     @default("{}") @map("default_scope") @db.JsonB
  initialTaskTemplates       Json     @default("[]") @map("initial_task_templates") @db.JsonB
  stageTaskTemplates         Json     @default("{}") @map("stage_task_templates") @db.JsonB
  isActive                   Boolean  @default(true) @map("is_active")
  createdAt                  DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                  DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([organizationId, businessUnitId, name])
  @@index([organizationId, businessUnitId, isActive])
  @@map("delivery_project_templates")
}

model DeliveryProjectTemplateProduct {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @map("organization_id") @db.Uuid
  templateId     String   @map("template_id") @db.Uuid
  productId      String   @map("product_id") @db.Uuid
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  @@unique([templateId, productId])
  @@index([organizationId, productId])
  @@map("delivery_project_template_products")
}

model DeliveryProject {
  id                  String                 @id @default(uuid()) @db.Uuid
  organizationId      String                 @map("organization_id") @db.Uuid
  businessUnitId      String?                @map("business_unit_id") @db.Uuid
  companyId           String?                @map("company_id") @db.Uuid
  primaryContactId    String?                @map("primary_contact_id") @db.Uuid
  sourceDealId        String?                @map("source_deal_id") @db.Uuid
  templateId          String?                @map("template_id") @db.Uuid
  pipelineId          String?                @map("pipeline_id") @db.Uuid
  stageId             String?                @map("stage_id") @db.Uuid
  idempotencyKey      String                 @map("idempotency_key") @db.VarChar(240)
  name                String                 @db.VarChar(200)
  status              DeliveryProjectStatus  @default(NOT_STARTED)
  healthStatus        DeliveryHealthStatus   @default(ON_TRACK) @map("health_status")
  priority            DeliveryPriority       @default(MEDIUM)
  ownerUserId         String?                @map("owner_user_id") @db.Uuid
  createdByUserId     String?                @map("created_by_user_id") @db.Uuid
  expectedStartDate   DateTime?              @map("expected_start_date") @db.Date
  kickoffDate         DateTime?              @map("kickoff_date") @db.Date
  expectedPublishDate DateTime?              @map("expected_publish_date") @db.Date
  actualPublishDate   DateTime?              @map("actual_publish_date") @db.Date
  completedAt         DateTime?              @map("completed_at") @db.Timestamptz(3)
  pausedAt            DateTime?              @map("paused_at") @db.Timestamptz(3)
  pauseReason         String?                @map("pause_reason")
  nextAction          String?                @map("next_action") @db.VarChar(240)
  nextActionDate      DateTime?              @map("next_action_date") @db.Date
  nextActionOwnerId   String?                @map("next_action_owner_id") @db.Uuid
  blocker             String?
  lastActivityAt      DateTime?              @map("last_activity_at") @db.Timestamptz(3)
  handoffStatus       DeliveryHandoffStatus  @default(DRAFT) @map("handoff_status")
  scopeVersion        Int                    @default(1) @map("scope_version")
  scopeSyncStatus     ScopeSyncStatus        @default(SYNCED) @map("scope_sync_status")
  scopeSnapshot       Json                   @default("{}") @map("scope_snapshot") @db.JsonB
  handoffChecklist    Json                   @default("{}") @map("handoff_checklist") @db.JsonB
  createdAt           DateTime               @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt           DateTime               @updatedAt @map("updated_at") @db.Timestamptz(3)
  deletedAt           DateTime?              @map("deleted_at") @db.Timestamptz(3)

  items        DeliveryProjectItem[]
  handoffs     DeliveryHandoff[]
  stageHistory DeliveryProjectStageHistory[]

  @@unique([organizationId, idempotencyKey])
  @@index([organizationId, businessUnitId, status, deletedAt])
  @@index([organizationId, ownerUserId, status])
  @@index([organizationId, sourceDealId])
  @@index([organizationId, expectedPublishDate])
  @@map("delivery_projects")
}

model DeliveryProjectItem {
  id                         String             @id @default(uuid()) @db.Uuid
  organizationId             String             @map("organization_id") @db.Uuid
  businessUnitId             String?            @map("business_unit_id") @db.Uuid
  deliveryProjectId          String             @map("delivery_project_id") @db.Uuid
  sourceDealLineItemId       String?            @map("source_deal_line_item_id") @db.Uuid
  splitKey                   String             @default("default") @map("split_key") @db.VarChar(80)
  productId                  String?            @map("product_id") @db.Uuid
  productCodeSnapshot        String?            @map("product_code_snapshot") @db.VarChar(80)
  productNameSnapshot        String             @map("product_name_snapshot") @db.VarChar(180)
  quantitySnapshot           Decimal            @map("quantity_snapshot") @db.Decimal(18, 4)
  revenueAmountSnapshot      Decimal?           @map("revenue_amount_snapshot") @db.Decimal(18, 2)
  grossProfitAmountSnapshot  Decimal?           @map("gross_profit_amount_snapshot") @db.Decimal(18, 2)
  customFieldsSnapshot       Json               @default("{}") @map("custom_fields_snapshot") @db.JsonB
  deliveryStatus             DeliveryItemStatus @default(NOT_STARTED) @map("delivery_status")
  createdAt                  DateTime           @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                  DateTime           @updatedAt @map("updated_at") @db.Timestamptz(3)

  deliveryProject DeliveryProject @relation(fields: [deliveryProjectId], references: [id], onDelete: Cascade)

  @@unique([organizationId, sourceDealLineItemId, splitKey])
  @@index([organizationId, deliveryProjectId])
  @@index([organizationId, productId])
  @@map("delivery_project_items")
}

model DeliveryHandoff {
  id                String                @id @default(uuid()) @db.Uuid
  organizationId    String                @map("organization_id") @db.Uuid
  businessUnitId    String?               @map("business_unit_id") @db.Uuid
  deliveryProjectId String                @map("delivery_project_id") @db.Uuid
  submittedByUserId String?               @map("submitted_by_user_id") @db.Uuid
  assignedCsUserId  String?               @map("assigned_cs_user_id") @db.Uuid
  status            DeliveryHandoffStatus @default(DRAFT)
  handoffSnapshot   Json                  @default("{}") @map("handoff_snapshot") @db.JsonB
  checklistSnapshot Json                  @default("{}") @map("checklist_snapshot") @db.JsonB
  submittedAt       DateTime?             @map("submitted_at") @db.Timestamptz(3)
  acceptedAt        DateTime?             @map("accepted_at") @db.Timestamptz(3)
  acceptedByUserId  String?               @map("accepted_by_user_id") @db.Uuid
  rejectedAt        DateTime?             @map("rejected_at") @db.Timestamptz(3)
  rejectedByUserId  String?               @map("rejected_by_user_id") @db.Uuid
  rejectionReason   String?               @map("rejection_reason")
  version           Int                   @default(1)
  createdAt         DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)

  deliveryProject DeliveryProject @relation(fields: [deliveryProjectId], references: [id], onDelete: Cascade)

  @@unique([deliveryProjectId, version])
  @@index([organizationId, businessUnitId, status])
  @@map("delivery_handoffs")
}

model DeliveryProjectStageHistory {
  id                String    @id @default(uuid()) @db.Uuid
  organizationId    String    @map("organization_id") @db.Uuid
  businessUnitId    String?   @map("business_unit_id") @db.Uuid
  deliveryProjectId String    @map("delivery_project_id") @db.Uuid
  fromStageId       String?   @map("from_stage_id") @db.Uuid
  toStageId         String?   @map("to_stage_id") @db.Uuid
  changedByUserId   String?   @map("changed_by_user_id") @db.Uuid
  enteredAt         DateTime  @default(now()) @map("entered_at") @db.Timestamptz(3)
  exitedAt          DateTime? @map("exited_at") @db.Timestamptz(3)
  durationMinutes   Int?      @map("duration_minutes")
  note              String?
  createdAt         DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)

  deliveryProject DeliveryProject @relation(fields: [deliveryProjectId], references: [id], onDelete: Cascade)

  @@index([organizationId, deliveryProjectId, enteredAt])
  @@index([organizationId, toStageId, enteredAt])
  @@map("delivery_project_stage_histories")
}
